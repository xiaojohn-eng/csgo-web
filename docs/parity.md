# 原作还原进度与验收边界

用户目标：完整复刻 CS:GO 网页版，可局域网联机，人物动作、枪械、特效、皮肤一样。此目标仍未完成。当前独立工程是继续开发的可玩基础，不能用功能测试通过代替原作逐项对照。

| 范围 | 当前实际覆盖 | 距离原作仍缺少 |
| --- | --- | --- |
| 局域网 | 同源页面与 WebSocket、10 人席位、AI 补位、房间列表与代码、重连、管理员移交；**原版竞技经济与回合计时**：开局 800 / 上限 16000 / 回合结束补 0、冻结 15 s、购买 20 s（比冻结多 5 秒进入实战）、回合 115.2 s、结算面板 3 s、击杀 300、下包/拆包各 300、团队奖金按获胜方式 3250/3500/3500/3250、下包但被拆 800、败方 1400+500×连败级阶梯；**原版赛制**：按 `mp_match_can_clinch` 赢过半数即结束（短竞技先到 9 局）、按 `mp_halftime` 在第 8 回合后换边且已赢回合跟着人走；**房间可选择原作两种赛制**：短竞技（16 回合/先到 9/第 8 回合换边，局域网默认）与完整竞技（30 回合/先到 16/第 15 回合换边），模式随快照下发、HUD 与商店跟随权威，未知赛制 id 被拒（`RULES_MISMATCH`）。全部取自发货 `csgo/cfg/gamemode_competitive.cfg` 与其短局覆盖文件，带行号与文件 SHA 回执（`docs/source-gamemode.md`）；**购买区限制**：购买区限制（**已按原数据接进游戏并真机验收**：Dust2 的两个 `func_buyzone` 从碰撞件的 `sensors` 取体积、`teamnum` 从地图自己的实体块按 hammerid 对表——2568320=2（T）、2568324=3（CT），三处独立事实互相对上（实体 `teamnum`、`info_player_terrorist/counterterrorist` 原点按碰撞件声明的转换落进各自区、`level.json` 出生点）；只接受能精确判定的体积（顶点恰好八个角=长方体，带旋转/缩放/非长方体/缺 teamnum/覆盖不全一律拒绝），见 `game/source-buy-zone.ts`、生成表 `game/source-buy-zone-data.ts`、`tests/source-buy-zone.test.ts`。购买区作为**场景自己的数据**（`SourceScenario.buyZones`）下发，本机训练局与服务端房间各带同一张表，`Simulation.canBuy(p)` 判窗口 + 区、`buy()` 与快照 `canBuy` 同源，商店按钮直接跟快照走；场景没声明区（移植自带脚手架/夹具）就没有这条规则可施加。另修一处同源问题：中途加入的玩家现在**在 `addPlayer` 时就入位到本队真实出生点**（此前停在原点直到下一回合）。真机（真实浏览器 + 局域网服务 + 训练局）验证：出生点 `canBuy=true`、向 W/D/S/A 走出 **11.63 m** 后 `canBuy=false`（购买被拒）、沿原轴走回后 `canBuy=true`，无页面错误（`output/playwright/source-buy-zone-ingame.json`，截图 `source-buy-zone-inside.png`／`-outside.png`）） | 少打多补偿（**值已入库**：发货竞技配置写 `cash_team_bonus_shorthanded 1000`、其它模式写 0，现已随模式表入库；但**没有任何发货文件写它由什么触发**，因此端口不施加它、也不替它编一个触发条件）；第二台物理设备与不同浏览器验证（**逐武器击杀奖励表已实现**：见 [原作击杀奖励](source-kill-awards.md)，AWP 100 / SMG 600 / 霰弹 900 / 刀 1500 / 电枪 0 / 其余 300） |
| 人物 | 原 T/CT 两套第三人称骨架、AK/M4/Glock/USP/Deagle 各自移动/瞄准/射击连续姿态及世界枪骨骼合并；双方独立第一人称手臂，CT 为原 IDF 卡其袖及手套；原 jump_lower 9-way 序列（move_x/move_y 3x3 混合、原 0.2s 淡入淡出）经空中层叠加接入全部 12 套武器姿态数据，原地跳/移动跳/移动中坠落均由 grounded 驱动，双客户端 LAN 逐样本一致（`output/playwright/source-jump-lan-evidence.json`）；原 Death1 死亡序列（T 96 帧/CT 101 帧 @30fps 非循环）单次播放至 cycle=1 后末帧锁定、参数中性、尸体开火层清零，接入全部 12 套武器姿态数据，服务器权威死亡姿态同步，双客户端 LAN 逐样本 cycle 零偏差（`output/playwright/source-death-lan-evidence.json`）；原 Shoot_GREN1 投掷序列 5 个移动变体（Idle/Walk/Run/Crouch_Idle/Crouch_Walk，各 14 帧 @30fps delta 层）按移动状态自动选变体，cycle 30/13≈2.307 cycles/s 推进至 1 末帧锁定、原淡入淡出，服务器确认投掷瞬间武装，接入全部 12 套武器姿态数据，双客户端 LAN 逐样本 cycle 零偏差（`output/playwright/source-grenade-lan-evidence.json`）；原 PHY ragdoll（16 部件质量/阻尼/关节轴限）在姿态管线的 Source 原生坐标系（+Z 向上）以位置动力学在服务器权威模拟，阵亡瞬间按击杀方向施加冲量、重力下落、地面接触与摩擦、关节锥软化，落地平躺后 freeze，接入全部武器驱动，双客户端 LAN 逐样本静止姿态零偏差且经渲染骨骼审计（`output/playwright/source-ragdoll-lan-evidence.json`）；**原作脚部 IK**：四条原 IK 链按 `GROUND` 规则与窗口在真实 Dust2 地表上求解（窗口外/平地零修正、超原台阶高被截断、不可达则拒绝），四个原武器系共用同一段代码与规则，真机 2029 帧中 188 帧开窗、最大修正 0.238m（`output/playwright/source-foot-ik-evidence.json`）；尸体**逐部件地面来自 Dust2 原碰撞**（每部件从自身球底向下射线，MASK_SOLID，排除传感器与 playerclip；空中阵亡会真正坠落到脚下地面、斜坡/台阶/箱体上按各自高度支撑、被天花板悬挂时以下方地面为准，无地表处保留死亡帧平面），权威端每 tick 刷新，证据 `output/playwright/source-ragdoll-terrain-evidence.json` | 原**脚部 `GROUND` IK 已实现**（按原作链/窗口/权重求解，平地零修正，见 [原作 IK 规则](source-ik-rules.md)；求解器本身是标准两骨 IK 而非原引擎实现；四个原武器系共用同一段代码，真机开窗样本只覆盖步枪系）、完整动作图（按武器区分的 `HandPos` 姿势层已于 2026-09-12 核对：它一直在生效，不再是缺口）、投掷完整变体（拉环/切枪投/低手投等，当前仅移动状态 5 变体）；跳跃上肢瞄准层完整对照；原版光照与角色材质完整对照；尸体与**其他演员/动态道具**的碰撞（当前只对静态世界查询）。**“最终握枪 IK”不是缺口**：原作玩家动画没有任何手部 `ATTACHMENT` 规则，`Idle/Walk/Run` 等层连一条 IK 规则都没有，1~3.8 单位的 `left_hand_attach` 间隙是原作动画自身的结果 |
| 枪械 | 原 AK-47、M4A4、Glock、USP、Deagle 第一/第三人称、原片段与两队持枪；原普通子弹伤害、准确度/散布/后坐/落地状态与预测重演；原 IN_SPEED 慢走；三种手枪的购买/独立弹药、提前换弹、模式、原动作声音通过训练与双客户端 LAN；Deagle 末发与耗尽42发后的原空仓声另经实际验证；**原版第三人称换弹图**（12 套武器数据各自的 `Reload_<weapon>` 包装 + 原上半身/下半身两条 auto-layer，原 0.1096/0.8904 包络）按武器自身换弹计时驱动、完成即弹匣提交、中断按原 fade 时间淡出，机器人依原 `botInput` 弹药纪律换弹；世界武器的**原弹匣网格**由该数据集自带的原显示事件 `AE_CL_EJECT_MAG`/`AE_CL_EJECT_MAG_UNHIDE` 驱动隐显（AK 0.2192~0.3288、M4 各队不同）；同一事件把**原版弹匣几何**作为独立地面道具掉落（原 `sv_gravity 800` 弹道、落地平躺、按原寿命移除、固定 8 槽渲染预算，材质按槽克隆），五套原资产（AK/Glock/USP/Deagle/AWP）单测交叉核验蒙皮变换、双客户端 LAN 逐项验证（`output/playwright/source-reload-lan-evidence.json`，姿态与弹匣回放 `scripts/verify-reload-pose.ts`）；**每发在模型自己的开火序列里抽一个**：两把步枪的原 `ACT_VM_PRIMARYATTACK` 各带三条等权序列（AK `ak47_fire1/2/3`、M4A1 `shoot1/2/3`；手枪里 USP/Deagle 同样各三条、Glock/AWP 各一条），而冻结的导出器当初每个 activity 只留了一条绝对序列，因此本轮把这批序列**按字节保留方式追加**进四个已发货第一人称资产（`scripts/append-source-rifle-fire-variants.py`：旧二进制仍是新文件的逐字节前缀、旧节点/蒙皮/网格/材质/图片/采样器/动画/访问器/缓冲视图全等；枪骨帧取自同一 ANI、同一套已验的 FRAMEANIM 解码链，手臂沿用导出器自己的合并规则——同名骨跟随武器、未匹配的 `Bip01` 保持自身静止姿态；导出器的根坐标约定是从**已发货的那条变体**反解出来再断言的，不是假设的），逐通道最大偏差 9.5e-06，两个身份与验证结果记在受版本控制的 `research/source-weapon-fire-variants.json`；端口侧只有**一个**共享抽取规则（`game/source-weapon-fire-draw.ts`），第一人称视图模型与手枪姿态时钟共用它，第一条序列始终是默认那条，装载器断言模型自己的整组序列都在（少一条即拒绝装载）；因为手枪的序列落在共享姿态状态里，那次抽取用**客户端指令种子**（两侧同种子，权威与预测逐帧一致），而第一人称视图模型是本机表现，按原作在本地抽。真机（真实浏览器 + 局域网服务）连发 30 发读到三个变体各 7/12/11 次、每发只跑一个变体、无页面错误（`output/playwright/source-rifle-fire-variants-ingame.json`） | 手枪 alt 姿态（USP 世界模型的 `pistol_fire_alt` 与蹲姿 alt 一组）、原作其他武器、完整运动/穿透仍缺；掉落弹匣的**停靠面按原碰撞射线取实际地表**（权威端换弹期间下发 `sourceGroundY`，客户端渲染侧再用预测用的原关卡把弹匣**自己所在列**的面逐帧重采样），飞行仍是单接触近似、不会沿地形翻滚下滑 |
| 特效 | 原手枪枪口/抛壳附件矩阵、USP 消音器第二枪口及本地第一人称原 main/core 粒子接入；原随机表和调度已移植；消音状态选择和预测/权威只触发一次经实射验证；**远端第三人称枪口**：手枪系由原世界开火动作、步枪系由开火层各自判一枪，两套原系统（`weapon_muzzle_flash_pistol` 与 `weapon_muzzle_flash_assaultrifle`，后者覆盖 AK-47 与 M4A1）都按武器自己的原 `muzzle_flash` 附件在世界里画出原粒子图/原贴图，真实 LAN 三客户端同一轮里各画 3 次（`output/playwright/source-muzzle-lan-evidence.json`）；**步枪同一原系统的 `_glow` 辉光与 `_main` 滚动火焰也已按原数据绘制**——辉光 `radius 25`/`Color Fade` 窗口 0.7–1、火焰 8 粒沿派发器自己的控制点路径铺开（`pathLength 17.5`、`distance 0..19`、半径 12→4、alpha 按自身 `Alpha Random` 抽取结果缩放），**同一派发器挂的另一条火焰 `weapon_muzzle_flash_assualtrifle_flame` 也已按原数据绘制**（原拼写 `assualtrifle`，同形连续发射链，alpha 100..120、自己的 `color1`、半径 8..1，与原滚动火焰叠加），`Remap Initial Scalar` 的 `output is scalar of initial random range` 已由本 build 自身机器码执行定案（标志为 true 时缩放到目标字段自身的初始随机值、为 false 时直接赋值，证据 `native-initializers.json`），15 ms 特效另用确定性烘帧与游戏内同帧捕获留证（`output/playwright/source-rifle-muzzle-preview-*.png`、`source-muzzle-third-person-frame.png`）；**远端 AWP 第三人称枪口也已按原数据绘制**：`weapon_muzzle_flash_awp` 派发的猎枪连续火焰 9 粒（`1200/秒 × 7.5 ms`，位置随出生时间 0→29 单位、半径 15→1、alpha 0.5→0 缩放到自身抽取结果、寿命 0.025→0.0175 s）加其 3 粒加法辉光，四条 remap 的缩放标志逐条读取并断言，`Movement Lock to Control Point` 按其字段语义断言为满强度固定控制点下不可移动（fail-closed）；真实 WebGL 固定时钟烘帧 4/10/22 ms 三帧互不相同（`output/playwright/source-awp-muzzle-preview-*.png`）。换弹 `AE_CL_EJECT_MAG` 的原版弹匣刚体道具（原重力弹道 + 落地平躺 + 原寿命移除，确定性与所有权单测覆盖）；**原版弹着贴花与表面撞击音**：一张枪弹停在哪个面，那个面自己的材质就给出一张贴花和一段撞击音——整条链路按发货文件读通（材质的 `$surfaceprop` → `surfaceproperties_cs.txt` 的组与 `base` 继承 → `gamematerial`/`bulletimpact` → `decals_subrect.txt` 的 `TranslationData` 与 `Impact.*` 组与权重 → 每张贴花自己的 `$Material`/`$Pos`/`$Size`/`$decalscale`/`$decalScaleVariation`），面的来源用碰撞件自己写的键（brush 序号 / 置换 chunk / 静态道具 `.mdl` 头），覆盖 brush 1515/2471、置换 101/101、道具 1246/1258、28 种表面属性、20 个原撞击事件 83 条原波形、2 张原图集（256×512 bulletsheet 与 1024×512 mod2x），见 [原作弹着](source-impact.md)、`game/source-impact-table.ts`、`game/source-impact-decals.ts`、`game/source-impact-audio.ts`、`tests/source-impact.test.ts`；**贴花尺寸的约定是量出来的**（`texel 范围 × $decalscale`：76 张两种形态都有的贴花里 52 张两乘积逐位相等）；绘制按发货 `DecalModulate` 的 `2 × texel × 帧缓冲` 与图集自身的中性灰 128，权威端 `traceBullet` 把 `{点, 法线, 表面属性}` 随 `shot` 下发、抽签由 shot 身份派生（同一发各端画同一张）；真机 34 发画下 69 张、命中 plaster/metal/concrete 三种原表面并各自播出自己的原事件（57/1/11 次）、8 张互不相同的原贴花、落点与权威命中点差 ≤0.001 m、83 条波形全部校验、`errors=[]`（`output/playwright/source-impact-ingame.json` + `source-impact-ingame.png`）；**LAN 上另作一次**：22 发后射手与**一枪未开的观察者**各画下 **70 张**、逐项比对最近 64 张**相同**的弹着（按 表面|贴花|世界坐标 比对，缺口 0）、同样 5 种原表面/12 张原贴花/4 个原事件且次数逐项相同、两端都校验图集与 83 条波形、`errors=[]`（`output/playwright/source-impact-lan-evidence.json`）；**原版弹壳（抛壳）**：`items_game.txt` 的 `eject_brass_effect` 指向 `cs_weapon_fx.pcf` 的三条系统（步枪 `weapon_shell_casing_rifle`、AWP `.50cal`、手枪 `9mm`），三条系统的模型/重力/阻力/寿命/自转/初速/碰撞响应逐字段读出并入库（见 [原作弹壳](source-shell-casings.md)、`game/source-shell-casings.ts`、`tests/source-shell-casings.test.ts` 11 条）；**三个数字都是量出来的**——模型以**毫米**为单位（19.15 / 38.7 / 57.067 就是 9×19、7.62×39、.50 实弹长，同样三个弹壳在 `models/shells/` 另有一份英寸版，回退精灵半径 1.25/2.0/3.5 是第三条独立证据），速度分量属于**发射器自己的轴**（在 AK 抛壳骨上量出：直接用得到"右 0.82 / 上 0.41 / 前 0.40"，先过 Source→世界映射则变成"右 0.999 / 上 −0.03 / 前 0.04"即完全侧向零抬升），而该骨的基**带着模型的英寸→米缩放**（直接取四元数会得到模长 0.517、方向翻转的"旋转"）；每步按原 tick 1/30 s 走 `Movement Basic` 的实测式子，碰撞走**关卡自己的 `traceBullet`**（与子弹、弹着同一条链路）并按算子自己的 `amount of bounce`/`slide` 响应（法向掉头，写成只缩放会被单测抓到）；真机（真实浏览器 + 局域网服务 + 训练局）：3 波 ×4 发抛壳 55 枚、`refused={}`、初速 4.117 m/s 落在该系统自身区间、**landed 60 / surfaceHits 60**（只对地面采样不可能得到这个数）、藏掉弹壳后帧内三角形正好少 952（同帧自身漂移仅 2）、画出的弹壳尺寸与模型几何逐位相符、`errors=[]`（`output/playwright/source-shell-ingame.json`）；**原版曳光**：`items_game.txt` 的 `tracer_effect` 指向 `cs_weapon_fx.pcf` 的三条系统（步枪 `weapon_tracers_assrifle`、AWP `weapon_tracers_rifle`、手枪 `weapon_tracers_pistol`），三条系统的渲染器/运动/偏移/时长/半径/alpha/两色/淡出逐字段读出并入库（见 [原作曳光](source-tracers.md)、`game/source-tracers.ts`、`game/source-tracer-draw.ts`、`tests/source-tracers.test.ts` 7 条）；**轨迹就是射击线**（`move particles between 2 control points` 扫过的两点就是这一枪的起点与落点，所以记住粒子去过哪里与沿线画一条带子是同一条曲线，端口画后者，是精确的那条）；带子长度是原件自己的两个数合出来的（时长 × 速度后由 `max length 500` 截到 **12.7 m**，再由 `length fade in time` 在 0.101 s 内线性长满——真机逐帧量到 2.10→4.19→…→12.70 m，与斜率 125.7 m/s 相符），宽度是 `Radius Random` 的半径（半宽 3 单位 = 0.0762 m），材质 `particle_spark.vmt`（`spritecard`、`$additive 1`、`$splinetype 2`、32×64 贴图）逐字节校验；**四个淡出时间按飞行进度的比例读并写明**（当绝对秒数会比任何一枪的飞行都长、带子永不可见，且本 build 给 `end_fade_out_time` 的缺省正好是 1——读法与边界一起写进表的 `fadeTimeBasis`，算子算术未执行）；一枪只画一条（预测与权威是同一发）；真机（真实浏览器 + 局域网服务 + 训练局）：55 m 的一枪飞 0.167 s，抓到 6 条带子**逐帧记录**、32 帧在视野内、带子从 2.10 m 长到 12.70 m、每条都在自己那一枪的线上（起点偏 0.508 m = 系统自己的 20 单位、终点与权威落点差 0）、速度 330.2 m/s / 半宽 0.0762 m / alpha 0.648 全落在系统自己的区间、写出的顶点量回 12.70000016 m、手枪一枪用 `weapon_tracers_pistol`、`refused={}`、`errors=[]`（`output/playwright/source-tracer-ingame.json`） | 步枪同一原系统的枪口烟/抛壳口烟（四条烟系统的**全部原参数**已按本 build 自己的原生缺省读出、三张原图与两份图集字节已入库并逐个校验，`$dualsequence` 的混合语义也已从客户端随包下发的参数文档量出：`$sequence_blend_mode 1` = 取第一条序列的 alpha、第二条的 rgb；**`Movement Basic` 的位置更新已改为"执行原算子"实测**（`scripts/probe-source-movement-basic-apply.py`：8 组用例 × 4 lane × 3 轴，`output = position + dragFactor·(position − previous) + gravity·dt²`、`dragFactor = (1−max(drag,0))^(30·dt)·(dt/tickInterval)`，逐通道最大偏差 1.46e-05，未被写的那一槽在所有用例里逐位不变，见 `research/source-movement-basic-apply.json`）；仍缺的是**给初速的初始化器如何把这一对位置播种**：`Position Within Sphere Random` 的实例与 apply（工厂 `0xcff480`、apply `0xd02980`）已定位并可直接调用，但它在合成集合上**不写任何东西**（写到日志里的三次都来自 post-unpack）——初始化器填的是发射器刚建出的粒子，需要先有那个上下文；下一步已按既有算子执行器路线试到底并**量出结论**：原发射链本身能跑通（手枪 `main` 的发射器与新粒子初始化都返回），但**在合成集合上一个粒子也留不下**——集合里逐字段的指针表最终为空、探针自己给的缓冲两次运行都是零；初始化器的 apply（`0xd02980`）只通过 `COL+0x68/0x6c/0x74/0x78` 四个字段取它要填的新粒子，并对取到的对象调 `[eax+0x98]` 虚函数。也就是说卡点不是"再测一个算子"，而是要**复现客户端由粒子文件建出的系统定义**（元素表、逐字段表、新粒子管线）。证据 `research/source-position-initializer-seeding.json` 写明这一点与下一步，未读出前四条烟仍不画）、原 smoke/sparks、深度羽化/HDR、**弹着贴花的雾衰减**已按发货程序自己的算式画出并逐像素验收**（`DecalModulate` 的 4 条静态组合逐条读出：雾量 = 地图自己的上限坡道、染色 = `(saturate(雾量×$FOGSCALE)^$FOGEXPONENT)²`、淡出 = `(雾量−$FOGFADESTART)/($FOGFADEEND−$FOGFADESTART)` 饱和后把 texel 拉向 0.5，距离按眼睛到片元；`decals_bulletsheet.vmt` 写 `$fogfadeend 0.5` 的那张面淡出、`decals_mod2x.vmt` 什么都不写的那张走发货的"无淡出"程序；两个采样编码假设都真机算过，存储值那条差 0.002–0.004、解码到线性那条差 0.065–0.148，见 [原作弹着](source-impact.md)）、原 `Subrect` 深度羽化未读、原 `r_decals` 池（发货默认 **2048**、分永久/动态两类、满则回收槽位；本移植用自己 512 张的有界池并如实记为差异，因为它每个弹着一张 quad）、"打到动态物件表面"（当前只画在静态世界 brush/置换/静态道具 PHY 上）；AWP 那条自带的烟与抛壳烟；**AWP 枪口的房间内样本**（首局 800 资金买不起 AWP，验收脚本没有买入路径，见下）；燃烧弹和复杂烟火持续性能 |
| 皮肤 | **原版涂料套件目录已按原数据入库**：6 把已实现武器的全部 **238 个涂装**（AK 45 / M4A1 39 / Glock 45 / USP 35 / Deagle 35 / AWP 39），每个都带**原作自己的中英文名**、**原作稀有度（含原作本地化文件里的档位名）**、`style`、`pattern`、`seed`、原磨损窗口、四色调色板、`phong*`、pattern 变换与原作解析出的 pattern/normal 贴图路径，逐字段校验 + 入库字节 SHA 回执，见 [原版皮肤目录](source-paint-kits.md)；原默认五枪与 AK 红线候选；原 style7 两路 shader、seed→UV、实际磨损参数进入菜单/保存/训练/三浏览器 LAN；其他玩家原世界枪同步和恢复默认已通过 | 原 clone 高光参数与指数尺寸修正正在对照；原过滤/mip/最终输出和完整材质、其他皮肤及贴纸；**合成材质的两个外推点已逐武器列表记录**（`docs/source-paint-kits.md` 末节）：分支一律用 AK 涂装态已验的 `albedoTint` 那条（各武器涂装态的真实分支未独立验证），第一人称与世界模型共用第一人称那份 VMT 的取值（AK/Glock 与世界材质逐项相同、M4A1 差 albedo 分支、AWP 强度差一倍、USP-S 世界材质是 HalfLambert+白 tint 的另一条分支）；**AK 全部 24 张 style7 涂装的原图案贴图已抽取入库并逐张验证无损**（24 pattern + 6 normal，两遍独立抽取对 kit 282 得到同一字节），且**整条链路已按涂装驱动**：18 张可合成涂装可在**武器库**里选中并装上武器，各自用**自己的**图案贴图与**自己的** Phong 值、强制**自己的**原版磨损窗口，客户端与服务端共读同一张生成表（`game/source-finish-table.ts`）；**已有六把枪共 77 张（AK 18 / M4A1 13 / AWP 12 / Glock 13 / USP 17 / Deagle 4，跨 style 1/2/7）**走这条路径，即这个移植出的每一把枪都能画自己的原版涂装（图案的四个数按原客户端自己的抽取：scale 是套件单值，offset/rotate 各从套件自己的 `*_start/_end` 区间抽，顺序与客户端逐抽一致，证据 `scripts/probe-source-paintkit-transform.py`）。真机两层验证：浏览器合成 282/302/180 得三张互不相同且非空的颜色图；游戏内选**火神**装上的材质即 `rubber_ak47.vtf`、再选**红线**得到不同颜色图、同磨损下再选回火神颜色图完全相同（`output/playwright/source-ak-finish-ingame.png`，见 [原版皮肤目录](source-paint-kits.md)） | **六把枪的涂装都已进入传输规则与武器库菜单**：AK 18 / M4A1 13 / AWP 12 / Glock 13 / USP 17 / Deagle 4 张，`game/source-finish-table.ts` 按武器生成，`deferredNoStagedInputs` 为空。合成适配器用**每把枪自己的三个 Phong 值**——boost 2/2/2/1/**8**/1、albedo 35/25/40/35/**80**/40、菲涅尔 `[.83 .83 1]` 与 `[.8 .8 1]` 两套（全部从各枪自己的武器 VMT 读出；此前写死的 `.83` 对 AWP 与 Deagle 是错的，已改为读数据），材质名按武器取值（M4A1/AWP/Glock/USP 第一人称与世界两个名字、Deagle 两个名字大小写不同，**AWP 镜片材质不替换**；USP 消音器与枪身共用材质故一并替换），选择按武器各记一份、换枪时重发。真机（真实浏览器 + 局域网服务，一局内六把枪）验证：六张颜色图互不相同、各自用自己那张图案贴图与自己的三个 Phong 值，换回 AK 不碰菜单即恢复自己那张且颜色图完全相同（`output/playwright/source-finish-ingame-evidence.json`）；**24 张带 normal 的涂装**（AK 6 / M4A1 6 / AWP 3 / Glock 4 / USP 2 / Deagle 3）所缺的排列已定位到"带 normal 的 kit 选了哪个 static 位"（style 7 的**任何**排列都不声明 `NormalsSampler`，而 shader 有 `$bumpmap`/`$NORMAL`、客户端读 kit 的 `use_normal`），该位未量出前不列；另 2 张（M4A1 155/993）是原作自己没唯一定位 pattern；**其余 147 张涂装**（AK 21 / M4A1 21 / Glock 32 / USP 19 / Deagle 29 / AWP 25）经查**不是别的着色器**，而是同一族 `CustomWeapon` 的其它 static 排列（已逐 style 编译并对照 style 7 逐字节一致），现在缺的是三样输入——`MasksSampler`/`NormalsSampler`/`OSPosSampler` 对应的原纹理角色——与**调色板常量 c0..c2 的原打包规则**（必须从原客户端机器码量出，不能按常量名反推；**这一条已量出并改写**：`scripts/probe-source-camo-palette.py` 读出四色的**来源与形态**——套件记录的 `+0xbc/0xc0/0xc4/0xc8` 四处各三字节 (r,g,b)、相隔 4 字节，逐字节 `cvtsi2ss` 成 0..255 浮点存进 `this+0x9c4+12i`；上传函数把每一组乘上 **`0x1936e9c` 处那个正好是 `1/255` 的 float32**（按字节断言）再走 `[%f %f %f]` 写成 `$camocolor0..3`。也就是说**客户端侧根本没有"四色→三常量"的打包**，上一版那条说法撤回；仍未读的是这四个材质变量与三个着色器常量之间**引擎按寄存器号的绑定**，所以"哪个颜色进哪个常量"仍未定；**同一个函数还是"一张涂装的材质块生成器"**，按 style 分派读出**每个 style 各写哪些变量、值来自哪个字段**（分派前写 `$aotexture`←`this+0x4a8`、`$weartexture`←`this+0xb3c`；两段 style 专用块**都以 `jmp` 回到共享块**，所以四色与 `$wearprogress`/`$paintstyle`/三个 `*texturetransform`/三个 `$phong*` **每个 style 都有**；style 0 块另有 `$baseTexture`/`$maskstexture`/`$grungetexture` 并按套件 style 字段开 switch 加 `$painttexture`/`$postexture`/`$surfacetexture`/`$exptexture`，style 1 块按位掩码 `0x2a4` 加 `$painttexture`/`$maskstexture`），因此"某张涂装要哪几张原贴图"已可逐 style 列出；仍未读的是这些材质变量**如何进入程序的采样器与常量**。**而打包本身已不再是缺口——它是排列自己声明的**（`scripts/probe-source-camo-packing.ts` 读已入库的 `evidence.json`：style 1/2/3/5/6/8/9 的 colour 排列各声明 `c0=g_cCamo0_camo3r`/`c1=g_cCamo1_camo3g`/`c2=g_cCamo2_camo3b`，style 4 只声明 `c0`、**style 7 一个都不声明**，任何 exponent 排列都不声明；三个 float4 的 12 个通道正好是四色 × RGB，即每个常量 xyz 放一色、w 放第四色的一通道，且本仓库翻译器对能翻的排列核对通道用法一致——style 1/2/5 把 `cN.xyzw` 与 `cN.wwww` 分开读；**这解释了上一轮为何在 style 7 附近找不到打包**）；同一张表还给九个采样器起名，已入库角色覆盖六个，未覆盖的正是 **4 `MasksSampler`、6 `NormalsSampler`、7 `OSPosSampler`**）；排列数据本身已入库（`game/source-customweapon-programs.ts`，style 7 的 token 与已发布程序逐字节相同）：**style 1/2/4/5 只差调色板参数的打包（已量出它不在着色器参数代码里，只可能在客户端/绑定层）与 `MasksSampler` 纹理入库**，style 8/9（53 张）还要补 `rcp`/`rsq`/`cmp` 三个操作码，style 3/6（21 张）还要补 `pow`/`dp2add`、两张纹理（`NormalsSampler`/`OSPosSampler`）与它们直接读的 `c10`、且没有 exponent 排列；线上只带一条涂装字段，副武器各自有皮肤这一点还未实现；原 seed→wear/grunge 变换与最终输出的 mip/filter 未对照 |
| 地图 | 原 Dust2 进入共享模拟及双客户端；6,572,252 原三角形、修正 PHY 轴符号的原碰撞、NAV/PVS、原3D天空区域和相机；世界 HDR 基础层及2,325道具实例的原 VHV、plain、decal、tint、tint+decal 分支通过 GPU；新增134植被网格原静态光/treesway与env_wind，加上四种原门框/线束tint的181网格，总2,640实例；其中64 olive/48 sky明确采用no-CSM分支；地图自己那条**投影阴影通道**已复刻：全图没有 `env_cascade_light`，所以原作也不沿太阳投阴影——每个角色是**垂直向下**投在自己脚下地面上的剪影，色 128 灰、可达 72 单位（1.8288 m），算式就是随附 `shadowmodel` 程序里的 `1 + coverage × (modulation − 1)` 与 `DstColor × Zero` 混合，逐像素实测最暗系数 0.5 对地图自己的 0.502、质心偏移 0.037 m、背阳侧系数 1（`output/playwright/source-projected-shadows-ingame.json`）；地图自己那条**投影阴影通道**的证据由 `scripts/probe-source-projected-shadows.py` 逐条读出并入库（`research/source-projected-shadows.json`）；地图自己的 **142 条绳索**（电线/缆绳）也已读出并入库——`keyframe_rope` 109 + `move_rope` 33、全部 `cable/nuke_cable`、`DT_RopeKeyframe` 的 16 个成员与偏移、`CRopePhysics<10>`（十趟约束）与 21 个控制台变量各自的默认值，以及**连接关系**（120 条写出下一跳、22 条是链端、33 条链走到全部 142 条、一跳 2.44~40.48 m 中位 18.71 m；链之间有 11 次共享引用所以链端比 mover 少）与渲染器的顶点/索引算式（每点 3 个顶点、每段 `m_Subdiv+1` 个点）（见 [原作绳索](source-ropes.md)） | 完整世界/道具 shader，剩余特殊道具/其他远景材质分支、天空第二层云滚动（**原数据已读出并留证**：Dust2 天穹是**两个**材质，第二层就是漂移的云层 `nuke_clouds_002`／`unlittwotexture`，`$basetexture`=`nuke_clouds_003`、`$texture2`=`nuke_clouds_001`、`$alpha .35`、`$translucent 1`、`$nofog 1`，并带 `texturescroll` 代理：`$basetexturetransform` 的 rate `0.00209`、angle `80`、scale `3.5`，`$texture2transform` 全 0/1；材质来源 VMT 的 sha、客户端实现该代理的类名 `CTextureScrollMaterialProxy` 与偏移 `0x10ef51a` 一并记入 `research/source-sky-second-layer.json`。**已做（2026-09-12 21:40）**：该代理如何由 rate/angle/scale 组合出变换矩阵**已经量出来并接进游戏**——类 `CTextureScrollMaterialProxy` 的 `Init`/`OnBind` 在客户端自己的 32 位二进制里逐字读出（键的 camelCase/材质小写靠 KeyValues 忽略大小写对上；角度过 π/180 双精度常量再走 `sincos`（目标由 `.rel.dyn` 重定位给出）；`u/v = frac(curtime × rate × cos/sin)`，负值加客户端自己的 1.0；矩阵 `[scale 0 0 u; 0 scale 0 v; 0 0 1 0; 0 0 0 1]` 经 `SetMatrixValue` 写入，**角度只在偏移方向里、矩阵无旋转项**），见 `scripts/probe-source-texture-scroll-apply.py`、`research/source-texture-scroll-apply.json`、`game/source-texture-scroll.ts`；云层底图现在按实测律每帧漂移（真机两次读数与实测律在渲染器自己报出的时刻上**逐位相等**、漂移方向比 5.671281819617706 对 `tan(80°)` 差 1e-15，`output/playwright/source-sky-scroll-ingame.json`）。**仍未做**：该 shader 的静态旗标已读出、云层落在那条**读两个采样器**的分支（`0x1`，见 [原作天空](source-sky.md)），第二张贴图 `$texture2` 已按原 VTF 导出并接进渲染（`game/source-sky-cloud-loader.ts`、`game/source-sky-render.ts`），逐像素复现了 `tex0 × tex1 × c1`；剩下的是原 GPU 实际选的 mip／各向异性／混合状态（alpha 乘积读的 `c1` 已量出 = `g_DiffuseModulation` FLOAT4[1]；程序尾部的 `cLightScale`、地图自己的雾、以及地图自己的太阳方向/颜色与跟着视野走的太阳阴影都已施加，见 [原作环境](source-environment.md)）、`env_sun` 太阳精灵（**数据与身份都已读**：地图那一个 `C_Sun` 的**八对键↔成员**按构建自己的 datamap 逐对读出——`material`→`m_strMaterial` `0x4e0` … `overlaycolor`→`m_clrOverlay` `0x504`；并查明**地图写的 `glowdistancescale "0.1"` 在全部 18 个发货二进制里都不存在**（键逐字比较，故构建匹配不到它，属作者标注而非数据，端口按原样记下并写明不可用）；**仍未画**：缺客户端那条绘制算式（`m_nSize 32` → 屏幕尺寸、`m_nOverlaySize -1` 时叠加画不画）与 `sprites/light_glow02_add_noz` 的导出，见 [原作环境](source-environment.md)）、动态光与自动曝光、声场（**地图自己那 57 个 `env_soundscape` 与它们的定义已读出并入库**：18 个名字、40 个不同半径（76~478）、20 个定义块（1 个重名、15 个继承两个基座）、66 处波引用共 55 个文件、客户端加载定义用的清单及清单注释里的 DSP 预设表与声级表、以及服务端"人严格在球内即候选"这条门与类自己的 datamap、**每个听众收到的那个音频块**（`DT_Local` 里的 `m_audio.*`：八点坐标 + 声场号 + 掩码 + 实体号，写入者是 `0xa16ca0`）以及**提交前的那两道比较**（`1.0` 与一个栈槽、再加一个字节，两条比较都按字节断言）、**它们各自的表成员资格**（门是 `CSoundscapeSystem` 第 5 槽、调起遍历的驱动是第 15 槽，另外两条路是 `CTriggerSoundscape` 第 105/103 槽）、以及**遍历拿到的听众记录布局**（调用方在自己栈上搭出来，`+0x24` 那个字节由调用方清零、遍历命中时置 1、调用方回来据此动作）；**但第一道比较读的那个栈槽（`[rbp-0x74]`）没有任何指令写它**（函数整体解码 772 条指令首尾严丝合缝、无存储落在该槽、槽从未被取地址、无外部分支跳入、全二进制 60282 个函数扫描同样为负——四条都写成断言），所以上一轮"实体分数恰为 **1.0** 者胜"的写法**已撤回**；**构建自己那张声级表也已读出**（`.data.rel.ro` 的 `0x19ce6c0`，30 条 `{int dB; char* name;}`、24 条名字自带数字可自校验、全二进制只有三个读取者且只做 dB↔名字互转），并记下**清单注释与它在 `SNDLVL_TALKING` 上不一致**（注释 60、构建 80）、注释那列 `attenuation` **在任何二进制里都没有对应表**（属文档非数据）；**"同时包含听众的多个声场之间怎么分"与"把声级变成可听半径的算式"未读，故一条都没播**，见 [原作声场](source-soundscapes.md)）、投掷物路线及持续性能验收（投影阴影自身仍缺两处：原作按接收面离自己那张影子平面的高度做淡出、并让第一人称武器投自己的影子，本移植把剪影放在角色下方第一块面上、不按高度淡出也不画武器影子）、**142 条绳索的绘制**（地图的键、连接关系与跳距、客户端的 16 个成员、渲染器的顶点/索引算式与纹理映射、两个数（模拟点数与长度）的来路都已读出并入库——点数由地图的 `Type` 键定（`Type 0` → 10，142 条全是 0），长度是两个端点之间的三点距离截断成整数（一条路加 `m_Slack`、一条路不加，**哪一条是静态绳索的最终值未读**），类自己的虚表（`0x1a8d3e8`）与 `.eh_frame` 的函数边界把"这是绳索的代码"钉住，还记下一条"看着对但没人调用"的死代码；但 `Slack` 是长度还是百分比、重力与阻尼各自是谁、顶点的偏移量、十个点怎么在两个端点之间松弛未读，故一条都不画——`load_ropes=False`，见 [原作绳索](source-ropes.md)） |
| 性能 | 真实骨骼 CPU 优化；静态道具与 world（BSP 面片）批处理+GPU 可见性剔除落 release 生产版（dev 实测 run 2→61FPS、draw calls 1176→648、JS 堆 -52%；world 批处理 82 网格并 56 组、PVS 集群切换 CPU 成本约 10 倍降低且渲染一致性/帧率零回归，证据 `output/playwright/source-worldbatch-perf.json`），生产 27019 上 USP 训练与双客户端 LAN 真实浏览器回归通过，844/844 测试全绿；10 人满员 10 分钟持续战斗压测通过（全程 60FPS、p95≤18.8ms、0 帧超 50ms、堆 -0.6% 零泄漏、0 页面错误，证据 `output/playwright/source-r5-soak-10p.json`） | 天空通道与动态物体未批处理；道具批处理组数（VHV 114/普通 84）待纹理图集或材质状态合并压缩；烟火叠加、低配目标设备 p95/p99 完整验收 |

R4 命中判定采用两套原模型共 44 个旋转 OBB，原 68 字节 hitbox 记录由本构建服务器程序交叉核验。440 条实际原 CPU 射线的头部判定一致，距离最大误差约 0.544 微米；439 条盒编号一致，另一条为原浮点同距的上下臂交界。真实 Simulation 已通过双方站/蹲射击和历史 SourcePose 回溯。普通 AK/M4 伤害另用 1,260 组原机器指令回读验证了浮点运算顺序、部位与距离系数、护甲消耗及最终取整，并已接到真实命中路径；这不包含重甲、穿透；原散布和后坐已另按原状态机接入，见 [射击状态说明](source-handling.md)。证据见 [伤害说明](source-damage.md)、`output/tests/source-damage-native.json` 和 `source-damage-verification.json`。

Glock 真实浏览器证据为 `output/playwright/source-r4-olive-gameplay-gpu.json` 和 `source-r4-olive-lan-gpu.json`：原 T/CT 手臂、单发20→19、三连发→16、AE54补至20/116而攻击门禁仍保留、主副武器独立、走蹲站、34+2音效SHA、风动134植被/总2459材质覆盖，两端无页面错误并清空房间。原命令/动画时钟/共享VM时间另见 `source-glock-command.md`、`source-glock-animation-clock.md`、`source-viewmodel-shared-clock.md`。

最新 USP 训练与 LAN 证据为 `output/playwright/source-r4-usp-gameplay-gpu.json` 和 `source-r4-usp-lan-gpu.json`：T 原手臂购买、CT 原默认 USP、两种音频/枪口、消音器提交与攻击门禁分离、AE54提前12/23、装卸取消重拔、检视、主副弹药、T 默认 Glock 回归；两端原姿态/仿真身份一致，errors=[]，rooms=[]。原手枪附件 102 个原帧与8原函数组合通过；现有粒子外形、弹壳模型和速度仍为通用实现。完整110文件627tests通过，详情见 [USP 记录](source-usp-simulation.md)。

PHY 原坐标轴纠正后，旧 CT 坡道卡住状态在48tick前进2.59361m，真实浏览器随后通过前进、蹲伏与ACK增长。M4 双浏览器 LAN 通过 CT 原手臂持枪、移动、换弹、检视、首次拔枪原音频及退出清房，见 `output/playwright/source-r4-m4-predecode-gpu.json`。两浏览器仍在一台 Mac 上；原始文件完整 SHA、各武器姿态与伤害版本共同进入仿真握手，拒绝旧数据客户端。

第二批 Deagle 训练、CT LAN 与实际42发耗尽证据分别为 `source-r4-deagle-gameplay-gpu.json`、`source-r4-deagle-lan-gpu.json`、`source-r4-deagle-empty-gpu.json`（均在 `output/playwright/`）。原 last-shot 活动、reload AE54、普通伤害 float32、独立 handling 与两端 pose 已接入；空仓声与最后购买 tick 竞态已有复现回归。本轮完整140文件751测试通过，见 [Deagle 接入](source-deagle-simulation.md)。粒子 main/core 的真实实射帧、USP 消音状态及 LAN 单次触发已通过；随后原 RNG/调度 R2 也已接入，深度羽化、烟雾/火花及完整最终视觉仍缺。红线真实操作、三客户端世界枪及服务端17类无效包检验见 [运行接入](source-redline-gameplay.md)，原合成与尚未关闭的材质差异见 [原合成链](source-redline-worktree.md)。

跳跃动画恢复点（2026-09-11 02:35）：原 jump_lower 序列（9-way 3x3，move_x/move_y 驱动）从原 FRAMEANIM 解码链提取并合并进全部 12 套武器姿态数据（rifle/M4/Glock/USP/Deagle/AWP × T/CT），空中由 grounded 驱动按序列自身 0.2s 淡入淡出叠加在地面状态之上，落地姿态冻结后淡出；原地跳（零向量 9-way）、移动跳（定向 9-way）、移动中坠落（走落台阶）三种形态在生产 27019 双客户端 LAN 上逐样本验证，双端 jump 层 weight/cycle/airborne 完全一致、跳高 1.524m、淡入淡出实测 0.2s，证据 `output/playwright/source-jump-lan-evidence.json`（含 129 帧采样时间线与三张截图）。冻结购买期（原 12s）内移动与跳跃按原版禁用，为验证时实测确认的服务器行为而非缺陷。

死亡动画恢复点（2026-09-11 04:35）：原 Death1 死亡序列（T 96 帧/CT 101 帧 @30fps，非循环）从原 MDL 提取并合并进全部 12 套武器姿态数据（新增 Death 状态、poseVersion 重写、研究快照同步打补丁+参考哈希刷新）。采样器与姿态驱动器新增 Death 状态：击杀后单次播放完整倒地动画、cycle 按 fps/(frames-1) 单调推进至 1 后末帧锁定、参数中性（move_x/move_y=0）、尸体开火层清零；死亡判定仅 `alive === false` 触发（修复 undefined 误判清零活人 fireWeight）；服务器权威推进死亡姿态并同步世界姿态（修复尸体渲染旧姿态）。生产 27019 双客户端 LAN 真实浏览器验证（两队交战、host 随队推进）：victim 全程 state=Death、3.2s 达 cycle=1 并持续锁定至采集结束、双端 70 对齐样本 cycle 逐样本零偏差、errors=[]，证据 `output/playwright/source-death-lan-evidence.json` + 倒地中/末帧锁定两张第一人称截图。全量回归 160 文件 837/837 通过（含新增死亡单测）。死亡 ragdoll 物理仍缺：当前为原版死亡动画序列播放（原版死亡本身先播 anim 再转 ragdoll，本实现止于动画），ragdoll 物理、投掷物动作、握枪 IK 是人物侧后续主线。

投掷动画恢复点（2026-09-11 11:40）：原 Shoot_GREN1 投掷序列 5 个移动变体（Idle/Walk/Run/Crouch_Idle/Crouch_Walk_Shoot_GREN1，各 14 帧 @30fps，非循环 delta 层，原骨骼权重蒙版下肢 0/上肢 1）从原 FRAMEANIM 解码链提取并合并进全部 12 套武器姿态数据（研究快照同步打 GREN 补丁，变体追加顺序曾与生产不一致致冻结哈希红，已剥离重打统一）。采样器新增 grenade 层（单次非循环 delta 叠加在瞄准层之上，插值路径支持层 lerp）；姿态驱动器在服务器确认的投掷瞬间武装投掷层，按当前移动状态自动选变体、cycle 按 30/13≈2.307 cycles/s 单调推进至 1 后末帧锁定、weight 按序列原 fadeIn/fadeOut 淡入淡出；`simulation.ts` 在投掷物生成瞬间触发（服务器权威）。生产 27019 双客户端 LAN 真实浏览器验证：live 阶段后 host 经真实按键路径投出 HE（站立 Idle→v831）、smoke（跑动 Run→v829）、flash（蹲姿 Crouch_Idle→v832），三段变体与每个采样点移动状态精确匹配、推进速率符合原版、末帧锁定、weight 包络完整、库存消耗正确、**38 个跨端匹配样本 cycle 逐样本零偏差**（时钟按投掷起点对齐）、errors=[]，证据 `output/playwright/source-grenade-lan-evidence.json` + 站立/跑动/蹲姿三张第一人称截图。全量回归 160 文件 844/844 通过（含 7 个投掷单测）。投掷完整动作图变体（拉环/切枪投/低手投等）仍缺：当前仅按移动状态 5 变体；死亡 ragdoll、握枪 IK 是人物侧后续主线。

死亡 ragdoll 物理恢复点（2026-09-11 17:00）：原 `tm_leet_varianta.phy`/`ctm_idf.phy` 的 ragdoll 定义（16 部件 bone/质量/阻尼/旋转阻尼/惯量/体积 + 15 个 `ragdollconstraint` 欧拉轴限）解码为 `public/source/csgo-12426148/ragdoll/ragdoll-data.json` 并接入全部武器姿态驱动（每套数据集各自建 index，同一场景共用一个尸体表）。死亡姿态由 Death1 cycle 0 帧确定性重建，不占网络带宽；运行时以位置动力学在**姿态管线的 Source 原生坐标系**（Source 单位，**+Z 向上**——渲染器再把它重定基到场景的 Y 向上帧，与动画采样器完全一致，故快照只传 16 个位置 + settled）模拟：自由积分 → 6 次 Gauss-Seidel 约束（距离按原部件质量分配、原关节锥松弛、地面投影）→ 由位置差重写速度并施加部件阻尼/地面摩擦/限速；部件朝向由关节链方向推导，服务器权威、客户端仅渲染。服务器在 `damage()` 里把游戏世界（Y 向上）的死亡速度与击杀冲量换算进该帧后再生成尸体。

本轮修掉两个只有到真机才暴露的缺陷（1/30 的旧单测都看不出来）。其一：**恢复期关节锥回正的位移被 `Δposition/dt` 重写成真实动能**，dt 越小放大越狠，1/60 下把尸体加速到 MAX_SPEED（800 单位/秒）并永久飞出地图（探针实测某队友尸体 68 秒漂移 4000+ 单位、垂直方向还持续爬升且永不落地）。其二（更隐蔽）：**模拟曾沿 +Y 施加重力，而该帧的垂直轴其实是 +Z**，于是尸体被沿真正的水平方向拖倒、Z 方向几乎不变，数值上"位置都在地面之上、两端一致、会 settle"全部自洽，渲染出来却是一个仍然竖立、悬空、头朝下的身体（真机骨骼世界 Y 跨度实测 2.361m ≈ 站立身高，而非躺姿体厚）。
修复为：约束只能改向或减速、不得加速（以自由积分速度为该 tick 能量预算）；已接触地面的部件由接触法向承担重量（消除重力每 tick 压入地面又被夹回产生的 p95≈0.2 单位/tick 弹道涟漪）；恢复期单次迭代修正上限 2 → 0.5（峰值抬升由 110 单位降到 ≤0.4）；静止判据由"最差单部件位移"改为"全部件平均位移 + 弹道项"，使被链条与地面夹住的单只脚不再无限阻塞 settled；重力、地面平面、摩擦与静止判定全部改用 +Z 垂直轴（`groundZ` 由死亡帧推出，约 -0.2 单位≈脚平面）。
验证：单测 14/14（含新增"权威 1/60 下 8 个击杀方向必须落地且 settle、峰值抬升 <80"与"静止后身体沿 Z 的跨度必须是体厚、且小于 X/Y 跨度"两条回归）；8 方向冲量 103~167 tick（中位 ≈120 tick ≈2.0s）内 settle，静止后逐 tick 位移精确为 0、Z 跨度 8.9~28.1 单位（0.17~0.54m 体厚，站立帧为 57.8）；六武器（Glock/USP/Deagle/AWP/AK/M4）多武器冒烟通过；全量回归 162 文件 858/858 通过。生产 27019 双客户端 LAN 真实浏览器验证：victim 灰岩双端均在 t=1.789s / 1.746s settle，骨盆沿 +Z 下沉 32.7 单位（旧错误轴下只有 14.3，因为根本没有真正倒下），80 帧对齐样本中 **45 帧静止姿态跨端最大偏差 0**（bit 级一致）、35 帧下落段速度感知容差零超差、poseVersion 一致、errors=[]；并由**渲染骨骼审计**确认静止尸体的角色骨骼世界 Y 跨度 0.311m、地面跨度 1.061m（旧实现为 2.361m ≈ 站立身高）、头与骨盆高差 0.066m、最低骨骼贴在死亡点脚平面（actorY -3.075）。证据 `output/playwright/source-ragdoll-lan-evidence.json` + 以真实相机对准尸体的下落中/静止两张截图（静止图可见尸体平躺在 Dust2 巷内、AK 落在身旁）。**仍缺**：ragdoll 当时只有演员局部一张水平地面（`groundZ` 由死亡帧推出），没有 Dust2 地形/楼梯/箱体/墙体查询——该缺口已在下面的"尸体地形碰撞恢复点"补齐；握枪 IK 是人物侧后续主线。

换弹掉落弹匣实体恢复点（2026-09-11 19:05）：原版换弹会把旧弹匣甩到地上（CS:GO 的 `CMagazine` 物理道具），本轮补上。世界武器的弹匣网格在原资产里**刚性绑定到唯一一个 `weapon_mag` 关节**（AK 125 顶点全权重 1，`matrixWorld`/`bindMatrix` 皆恒等），因此"卸下的弹匣"就是把**原版弹匣几何**按该关节的蒙皮世界矩阵做刚体变换，不需要第二份资源；该事实写成 `game/source-magazine-drop.ts` 的 `sourceMagazineMeshTransform()`，并在真实 AK/Glock/USP/Deagle/AWP 五套原资产上以 three.js 自身 `applyBoneTransform()` 蒙皮路径交叉核验（`tests/source-character-actor.test.ts`、`tests/source-*-character-owner.test.ts`）；非单关节刚性绑定、材质为数组或方向退化时**直接拒绝生成，不伪造形状**。

事件通道沿用原数据集自带的显示事件：四个武器系角色在采到 `AE_CL_EJECT_MAG` 让弹匣网格消失的**那一帧**（`magazineVisible` 由 true 落为 false）产出掉落请求，因此不需要新增任何网络字段——两端都由同一条权威 `player.reload` 时钟推导出同一事件。弹匣按 Source 原重力（`sv_gravity 800`，1 单位 = 1 英寸 → 20.32 m/s²）做有界弹道：初速沿武器前方/右侧/向上合成、确定性伪随机翻滚（同一 seed 两端同轨迹），落地按下沉方位求盒体支撑高度使其**永不穿地**，接触后按恢复系数/摩擦反弹，速度衰减到阈值即**倒向最大面平躺**（近似 VPhysics 的自动放平，非完整刚体解算），12 秒后按原 `m_flRemoveTime` 语义移除。渲染池固定 8 个网格预算、不投影阴影；材质按槽位克隆一份，避免武器换肤释放材质后地面道具指向已销毁材质。本地玩家自身的第一人称世界模型平时不可见，但换弹期间仍被采样，因此射手**也看得到自己那匣掉在地上**。

验证：新增 `tests/source-magazine-drop.test.ts` 12 条（刚性提取与拒绝、确定性、原重力、永不穿地、平躺、预算回收、材质克隆所有权与释放、原寿命移除）+ `tests/source-character-actor.test.ts` 3 条（真实 AK 资产上掉落只触发一次、掉落矩阵与蒙皮路径一致、无效权威不产出）+ 五套原资产各自的掉落断言；全量回归 165 文件 913/913、类型检查通过。生产 27019 双客户端 LAN 真实浏览器（`scripts/run-playwright-reload-validation.mjs` 扩展）：装载后 host 经真实 KeyR 换弹，本机在**原 `AE_CL_EJECT_MAG` 事件 ±0.13s** 内生成道具（本地玩家用自身预测姿态，天然略领先权威时钟），从 4.402m 落到**射手地面 3.045m 之上恰好 0.075m**（= 原弹匣几何最短半轴，即平躺在自己脚下），并在观察窗口结束前按寿命移除；同一轮里服务器按 PVS 把视线外的敌方 host 对 peer 隐藏（`y=-100`），peer 因此**正确地一件道具都不生成**（不是漏做）。**仍缺**：掉落弹匣是纯表现道具（原版亦然，不可拾取）；地面用射手脚下平面近似（无 Dust2 地形/箱体查询，空中换弹会停在起跳高度）；"视线内的旁观者看到远端换弹弹匣"未做真机对照（本 harness 两端各自留在出生点、服务器 PVS 互不可见），该远端路径与本地共用同一段代码并由上述原资产单测覆盖。

尸体地形碰撞恢复点（2026-09-11 19:40）：此前 ragdoll 只有**演员局部一张水平地面**（`groundZ` 由死亡帧推出），因此空中阵亡会在死亡高度悬停、尸体不贴合斜坡/台阶/箱体。本轮把每部件的地面换成**Dust2 原碰撞的逐部件查询**：`game/source-level.ts` 新增 `groundHeight(x,z,fromY)`（向下射线，MASK_SOLID 即本层的 `projectile` 角色，排除传感器与 playerclip；起点落在实体内返回 null 而不是 0 距离），`game/source-ragdoll.ts` 新增 `updateSourceRagdollGround()`：按渲染器同一条换算 `world = actorPos + scale·Ry(yaw+π/2)·(px, pz, −py)`（该换算已在真实骨骼上核对，误差 2.7e-15 m）把每个部件映射到世界，射线**从该部件自身球底稍下**出发——因此悬挂在天花板下的部件取的是下方地面而不是被天花板托起；射线起点已在实体内（部件嵌入几何）时，改从部件自身球顶稍上再查，把部件推回其所在实体的顶面。`SourceRagdollLive` 增加逐部件 `groundZ`，`stepSourceRagdoll` 不再接收单一平面，约束求解、接地摩擦、应变门控与静止判定全部按部件各自的地面执行；权威端 `simulation.ts` 每 tick 用 `sourceLevel` 构造查询并随 `beginRagdoll/stepRagdoll` 传入。无地表可查的部件（原始碰撞在某些内部列没有导出地表）保留**死亡帧平面**——即改造前的老行为，既不会凭空获得支撑，也不会让尸体坠出世界。

同时修掉一个被浅下落掩盖的物理缺陷：原速度重写把**整条速度**每 tick 乘 `VELOCITY_RELAX`，等价于把下落速度上限压到 `g·dt·relax/(1−relax) ≈ 15 单位/秒`（≈0.4 m/s）。尸体过去只在自己脚下 0.2 米内落下，这个错误看不出来；一旦要坠落到几米下的地形就完全不成立。改为：自由积分速度（含重力）继续累加，**求解器修正**单独作为一条被削弱的修正速度叠加（修正幅度上限为每 tick 重力增量，避免恢复期关节回正被重写成动能），水平两轴额外施加空气阻力（`HORIZONTAL_AIR_DRAG`），使被击杀冲量甩出的尸体仍落在死亡点附近而不是滑行数米。行为变更同步提升 `SOURCE_RAGDOLL_POSE_DRIVER_VERSION`（`r1`→`r2`），旧客户端在仿真握手上被拒绝。

验证：新增 `tests/source-ragdoll-terrain.test.ts` 6 条（逐部件地面取自真实地表而非单一平面、实心天花板不得托起尸体、playerclip 不支撑道具、空中阵亡落到脚下地面、无地表处保留死亡帧平面、全过程无部件低于自身地面）与 `tests/source-ragdoll.test.ts` 1 条（击杀冲量不得让尸体滑行），`tests/source-level.test.ts` 1 条（`groundHeight` 的 MASK_SOLID/传感器/实体内部语义）；全量回归 **166 文件 921/921**、类型检查通过。真实 Dust2 原碰撞离线验证 `scripts/verify-ragdoll-terrain.ts`（走权威同一 driver API）：空中 4 米阵亡**实测坠落 3.81 米**并停在原地面 3.0648m 之上 0.079m（=该部件半径）、跨真实 1.037 米台阶的尸体 16 部件读出 **4 个不同真实高度（跨度 1.1 米）**、无地表列保留死亡帧平面，证据 `output/playwright/source-ragdoll-terrain-evidence.json`。生产 27019 双客户端 LAN 真实浏览器（`scripts/run-playwright-ragdoll-validation.mjs` 扩展，采集器新增 x/z/yaw，并把录到的每一帧尸体位置**回放到真实碰撞**校验）：victim 流火双端 t=1.791s/1.743s settle、骨盆下沉 28.61 单位、距死亡点水平位移 75.5 单位、**44 帧静止姿态跨端最大偏差 0**、速度感知容差零超差、errors=[]；回放 1232 个部件地表：**42 帧静止尸体全部停在真实地表上、0 帧退回死亡帧平面、最深 0 米（从未进入地图内部）**。**仍缺**：尸体只与静态世界求交，不与其他演员/动态道具碰撞；被天花板悬挂的部件按"下方地面"处理而不是真正的接触法向；地面查询是每部件一次向下的射线，没有墙面法向与滑动（贴墙尸体由一发向上的解穿插处理）；握枪 IK 是人物侧后续主线。

原作 IK 规则导出恢复点（2026-09-11 20:10）：长期挂在缺口里的“最终握枪 IK”本轮被**证伪并换成真正缺的东西**。原作玩家动画模型（`t_animations.mdl` / `ct_animations.mdl`）声明四条 IK 链 `rhand` / `lhand` / `rfoot` / `lfoot`，每段动画的 IK 规则不在 `.mdl` 里而在 `.ani` 动画块（`animblockikruleindex`）；本轮按公开 Source SDK 2013 的 `mstudioikrule_t`（152 字节）逐条解码，并用记录自带的自检字段验证布局：536 个带规则描述符中**每一条**规则的 `index` 都等于其位置，`compressedikerrorindex` 也落在合理范围（4 条规则的块在 `@rifle_reload` 上正好读到 608 = 4×152）。每队共 1528 条规则：`RELEASE` 1156（四条链都有，窗口多为整段）、`SELF` 350（**只**出现在 180 个名字含 `HandPos` 的按武器姿势描述符上，只作用于两条手链）、`GROUND` 22（只作用于两条脚链，带真实周期窗口与 `contact` 帧，`height` 恒 18、`radius` 恒 2.5），**`ATTACHMENT`/`WORLD` 为 0**。结论：**原作没有把手锁到武器上的 IK 规则**——`left_hand_attach` 不被任何规则引用，我们已经在播的 `Idle_lower`/`Idle_Upper_AK`/`Idle_Shoot_AK` 更是一条规则都没有；`scripts/validate-source-world-ak.mjs` 记录的 1.3~3.8 单位间隙是原作动画自身的作者结果，按它去补 IK 反而会偏离原作（武器模型自带的 `weapon_hand_L` 骨骼离该附件 38.8 单位，说明该附件只是枪上的参考点、不是精度基准）。真正待实现的是**脚部 `GROUND` IK** 与**按武器区分的 `HandPos` 姿势层**。

数据落地：`scripts/export-source-ik-rules.py`（Blender + 固定 SourceIO，输出 `research/source-ik-rules.json`，含 `.mdl`/`.ani` 字节数与 SHA256 回执）与 `scripts/stage-source-ik-rules.py`（入库 `public/source/csgo-12426148/ik/ik-rules.json`，只保留发货姿态数据集真正会采样的描述符：两队各 56 个 / 190 条规则）。入库时逐个描述符与四个已冻结 `pose-data.json` 的 `ikRules` 计数**交叉核对**（每套各 178 条，全中）——两个独立产出的资产互相印证。运行时解析在 `game/source-ik-rules.ts`：校验格式/链名/骨名/规则类型/窗口单调/骨索引范围，提供按描述符取规则、取脚部地面规则，以及原窗口包络 `sourceIKWindowWeight()`（窗口可越过 1，按一个动画周期取模回绕）。验证：`tests/source-ik-rules.test.ts` 8 条（四条链的原骨名、无 `ATTACHMENT`、`GROUND` 只打脚链而 `SELF` 只打手链、原 18/2.5 数值与 `contact` 落在窗口内、与四套 `pose-data.json` 逐描述符计数一致、窗口包络含跨 1 回绕、畸形数据被拒）。细节见 [原作 IK 规则](source-ik-rules.md)。**仍缺**：IK 求解器本身；脚部 IK 还需要客户端渲染侧的地形查询（目前只有权威端 `Simulation` 持有 `sourceLevel`），`SELF` 规则搭配的压缩 IK 误差通道也未解码。

原作枪口效应表与第三人称枪口恢复点（2026-09-12 02:40）：本轮把“远端第三人称枪口/枪口烟/弹壳/曳光”从对照表的缺口推进到**原作数据在手**。唯一出处是 `scripts/items/items_game.txt`（6,791,631 字节，SHA256 `510e09b6…`）：每把武器的 `visuals` 块分别写 `muzzle_flash_effect_1st_person` / `muzzle_flash_effect_3rd_person` / `heat_effect` / `eject_brass_effect` / `tracer_effect` / `weapon_type`，原作**明确区分第一与第三人称系统**（SSG 08 `_huntingrifle_FP` vs `_huntingrifle`；Elite `_pistol_elite_FP` vs `_pistol_elite`），AK-47/M4A4 同为 `weapon_muzzle_flash_assaultrifle`、AWP 为 `weapon_muzzle_flash_awp`、Glock/USP/Deagle 为 `weapon_muzzle_flash_pistol`；弹壳 `_rifle`/`_50cal`/`_9mm`，曳光 `weapon_tracers_assrifle`/`_rifle`/`_pistol`，枪口烟统一 `weapon_muzzle_smoke`。导出脚本 `export-source-muzzle-flash-map.py`（KV 栈解析 + 字节数/SHA 回执）→ `research/source-muzzle-flash-map.json` → 入库 `public/source/csgo-12426148/weapon-effects/effect-map.json`（只含本作 6 把武器），运行时 `game/source-weapon-effects.ts` 严格校验、缺字段即抛错、不回退替代。第三人称枪口（手枪系）已实现：`game/source-world-muzzle-flash.ts` 只做决策（射击是跃迁不是电平、消音走原作空高位场不画、未入库系统上报而不顶替），`scene.ts` 用同一份原手枪粒子图再建一个世界场景实例，按每个远端角色自己的 `muzzle_flash` 原附件开火（`GameAssets.sourceAttachment()` 分发到 AWP/Deagle/手枪 owner 的世界武器附件；步枪 owner 无附件即返回 null），种子由「射手 id + 其第几枪」推导，两端索引进同一张原随机表。验证：`tests/source-world-muzzle-flash.test.ts` 7 条、全量 **170 文件 949/949**、类型检查与 lint 0 error；LAN 真实浏览器报出世界枪口渲染器已加载（`csgo-pistol-main-core-12426148-r2`，hash 已校验）且其余断言全绿，但该观测窗口内没有可见的持手枪远端开枪，故 `bursts=0`（接线已验证、渲染效果未取到样本）。**仍缺**：步枪/AWP 的原作第三人称系统名已确定但粒子图未导出/移植（当前唯一已移植的原系统是手枪的），因此远端步枪枪口仍不可见；弹壳与曳光、枪口烟当时只拿到原映射（弹壳与曳光其后已实现，见 [原作弹壳](source-shell-casings.md)、[原作曳光](source-tracers.md)；枪口烟仍未实现）。

远端第三人称枪口真机验收恢复点（2026-09-12 07:45）：本轮修正了 02:40 恢复点的一个**真实错误**——手枪系的**世界姿态驱动发布的是世界开火动作本身**（`action.activity === 192`），不是响应表里的 `fireWeight`/`fireCycle`，所以只按姿态字段判枪会漏掉全部手枪系远端开火。`game/source-world-muzzle-flash.ts` 因此重写为**两种触发形态**（`world-action` | `fire-layer`）并新增 `sourceWorldMuzzleTrigger()`，按每个角色自己的 rig 取它**真正发布的那一种**开火状态（Glock/USP/Deagle → 世界开火动作，步枪/AWP → 开火层）。判定“新一枪”的语义按形态区分并各由测试钉住：世界动作是**任意变化**（`|mark−cursor.mark|>1e-6`，动作每次开始都是一发），开火层只有 **cycle 回退**（`mark<cursor.mark−.02`）才算一枪（一个原动画内 cycle 只前进，用绝对值会把同一发内的周期前进误判成新射击）；游标初始为 `{firing:false, mark}`，使**中途出现**的角色仍能显示那一枪而不是丢掉它。真机验收脚本独立成型 `scripts/run-playwright-muzzle-validation.mjs`：**3 个真实浏览器客户端**（建房射手 + 先加入以占住另一侧的填充者 + 落在射手同侧的观察者；观察者回主菜单后仍在线，因此**继续渲染世界并接收权威快照**），射手按 `Digit2` 改用本作唯一已移植的手枪粒子图并开火 3 发，观察者读自己的 `assetAudit().sourceWorldMuzzle` 并与射手渲染出的 `matrixWorld` 位置交叉核对。结果 `MUZZLE VALIDATION PASSED`：**画出 3 次闪光、3 次全部由原世界开火动作归因**，`lastBurst` = `glock` / `weapon_muzzle_flash_pistol` / 第 3 发，锚点 1.71m，errors=[]；证据 `output/playwright/source-muzzle-lan-evidence.json`。`tests/source-world-muzzle-flash.test.ts` 扩到 9 条、全量 **170 文件 951/951**、类型检查与 lint 0 error。**本轮同时核实了尚未移植的步枪/AWP 的真实结构**：`weapon_muzzle_flash_assaultrifle` 是**父系统**（只有 `Set Control Point Positions` + 5 个 `DmeParticleChild`），真正的闪光在 `weapon_muzzle_flash_assaultrifle_vent`（材质 `particle_muzzleflashx.vmt`，instant emitter + `Position Within Sphere Random` + `Remap Noise to Scalar` + `Alpha Random`，算子 `Movement Basic`/`Lifespan Decay`/`Alpha Fade Out Random`），另有 `_flame` 连续火焰、`weapon_muzzle_flash_smoke_small2` 与两条 `weapon_shell_eject_smoke_assrifle*`；`weapon_muzzle_flash_awp` 挂 `smoke_small`/`smoke_small3`/`weapon_muzzle_flash_huntingrifle_main`/`shell_eject_smoke_awp3`。它们与手枪图**材质与算子都不同**（步枪/手枪的 renderer 材质分别是 `particle_muzzleflashx.vmt` 与 `particle_muzzleflash4.vmt`），必须各自导出与有界移植，不能用现有手枪图顶替。

原步枪第三人称枪口恢复点（2026-09-12 08:20）：上一条把步枪那条链路**从“结构已核实”推进到真正跑起来**。`scripts/export-source-muzzle-flash-particles.py` 遍历 `cs_weapon_fx.pcf` 中步枪与 AWP 两个根的闭包（21 系统 / 7 贴图 / 9 材质，逐文件字节 + SHA-256 回执），`scripts/stage-source-muzzle-particles.py` 只入库运行时真正会取的三样（闭包图、该闭包用到的原生算子缺省子集并记录未过滤原表 SHA、`muzzleflashx` 的 PNG）并写 `game/source-muzzle-particle-resources.json`。过程中修掉两个真实缺陷：① 原引用有一处**省略了 `.vmt` 扩展名**（`particle\fire_particle_4\fire_particle_4`），导出改为“唯一解析并记录 `materialResolutions`”，图校验按该记录核对而不是自行猜路径；② 多根闭包只从声明根遍历会让 AWP 分支被判「不可达」而整张图被拒——现在遍历**每个声明根**。另 `muzzleflashx.vtf` 是 **VTF 7.1（64 字节表头，早于资源字典）**，结构上不可能带 sheet，导出把这点写成 `resourceTable: absent-before-7.2`。移植 `game/source-rifle-muzzle-particles.ts` **逐相位枚举**原 vent 的算子集合，任何算子/材质对不上即抛错、不做部分应用；唯一未复刻项已明确声明：原半径由一条对**绝对世界时间**取样的平滑噪声场决定，而两端不共享绝对时钟，本移植改从原标量表按「射手 id + 其本人第几枪」取一项落在原 5–7 区间内的值（适配器，不是原噪声实现）。渲染 `game/source-rifle-muzzle-renderer.ts` 复用与手枪**同一份** `game/source-sprite-card.ts`（官方 spritecard 公式、原 `$additive 1`/`$depthblend 0`、原 128×128 贴图），锚在射手世界武器原 `muzzle_flash` 附件上；`scene.ts` 改为**按原系统名选渲染器**，并给 `GameAssets.sourceAttachment()` 补上步枪 owner 的分发（原先步枪角色根本没有附件通路）。同时把四个原 rig 的 `attachment()` 从「取不到就抛错」改成**返回 `null`**：渲染循环里一个只是还没就绪或被隐藏的角色不该把整帧打崩。验收：新增 `tests/source-rifle-muzzle.test.ts` 8 条（入库回执与闭包结构、vent 原参数逐项、单粒子与原区间、同种子同值、淡出与寿命、**任何算子/材质变化都被拒**、原生缺省被真正读取、staged 字节 SHA 与容量溢出）、`tests/source-character-actor.test.ts` 新增步枪附件契约（无姿态返回 `null`、附件世界点与它父到的渲染骨**误差 <1e-3 m**、错名字返回 `null`），三套手枪/AWP owner 测试补上「无姿态→`null`」；全量 **171 文件 960/960**、类型检查与 lint 0 error。真机（真实浏览器 + LAN 服务 + 3 个真人客户端）`scripts/run-playwright-muzzle-validation.mjs`：射手先 `Digit2` 用手枪、再 `Digit1` 用主武器各开火 3 发，观察者**两套原系统各画出 3 次**——`glock / weapon_muzzle_flash_pistol` 与 `vandal / weapon_muzzle_flash_assaultrifle`，锚点距射手 1.72 m / 1.63 m，errors=[]，证据 `output/playwright/source-muzzle-lan-evidence.json`；细节见 [原步枪第三人称枪口](source-rifle-muzzle-particles.md)。**仍缺**：步枪只执行 vent，同一原系统的滚动火焰、辉光、枪口烟、抛壳口烟与连续火焰未执行（原参数与原材质已随图交付，未用近似效果顶替）；**AWP 仍未入库**（原系统没有 vent，派发的是猎枪的连续火焰加两团烟，另一条原链路）；原派发器控制点、首步调度相位与逐 initializer 的 RNG 顺序未执行；原 SpriteCard 深度羽化/HDR 未复刻；弹壳与曳光、枪口烟当时只拿到原映射（弹壳与曳光其后已实现，见 [原作弹壳](source-shell-casings.md)、[原作曳光](source-tracers.md)；枪口烟仍未实现）。

原作脚部 IK 求解恢复点（2026-09-11 20:45）：上一恢复点把原作 IK 规则导出后确认“缺的是脚部 IK 而不是握枪 IK”，本轮把它实现并接进渲染。`game/source-foot-ik.ts` 按**原链定义**把有 `GROUND` 规则的两条脚链（`rfoot`/`lfoot` = `Bip01_*_Thigh → Calf → Foot`）解析到本骨架；新增 `sourceSequenceBlend()`（与 `sampleSourceSequence` 共用同一份 SDK 三向三角权重代码，避免两份逻辑漂移）取出当前姿态**真正由哪几个原动画混合而成**，再按各动画的原 `GROUND` 规则与**原窗口包络**（`sourceIKWindowWeight`，窗口可越过 1）加权出本帧的 `envelope/height/radius/contact`；修正量 `delta = clamp(envelope × (脚下地表 − 角色所站地表), ±height)`，`height` 就是原规则的 18 单位（0.4572m，也正是 Source 台阶高）。窗口外/平地/无地表**零修正**（未启用时不做射线也不碰姿态）；超过原台阶高截断。求解器是标准两骨解析 IK（余弦定理求膝位 + 两次世界旋转对齐髋/膝），保留动画自身的弯曲平面与脚骨世界朝向，髋不动、原骨长严格保持，不可达时**拒绝**而不是拉长骨头；途中修掉 `THREE.setFromRotationMatrix` 在带均匀缩放（1 单位 = 1 英寸 ×0.0254）的骨骼树下取出的世界旋转错误（修前膝/脚落点偏 0.11m，见 `sourceBoneWorldRotation()`）。接线：`loadSourceCharacter()` 同时取回并校验 `../ik/ik-rules.json`，运行时把预测用原关卡的地表查询注入 `GameAssets.resolveGround`，`animateOperator()` 在步枪角色系每次姿态后应用，结果写入 `root.userData.sourceFootIK` 供真机验证读取。

验证：`tests/source-foot-ik.test.ts` 6 条（只留两条脚链、按原窗口/权重混合出 18/2.5/0.45、平地+无地表+窗口外三态零改动、台阶上按窗口量抬起且骨长/髋位/水平位置/脚朝向不变、上下修正都被 18 单位截断、合成链上不可达目标被拒绝）；全量回归 **169 文件 941/941**、类型检查与 lint 0 error。真实 Dust2 原碰撞离线验证 `scripts/verify-foot-ik.ts`（走渲染同一 `applySourceFootIK`）：围绕真实出生点采样 **904 个真实列 × 4 个朝向**，修正量与“原窗口 × 两处地表差”**逐样本完全相等（最大误差 0）**、2 列跨过 ≥12cm 真实落差、65 列因不可达保持原姿态、单面（恒定地表）检查 `deltas=[0,0]`/`moved=0`/**骨骼差 0**，证据 `output/playwright/source-foot-ik-evidence.json`。生产 27019 双客户端 LAN 真实浏览器：**2029 帧渲染角色里 188 帧存在原作脚部规则开窗、185 帧真的移动了脚、最大修正 0.232m**（另 3 帧按设计拒绝），errors=[]、原换弹/弹匣/权威地表断言全部保持通过。**仍缺**：求解器是标准两骨 IK 而非原作引擎内部实现；只沿世界竖直方向修正（无墙面法向/滑动）；参考面取角色自身列的地表，站姿下摆动脚不修正；按武器区分的 `HandPos` 姿势层未应用；真机窗口内机器人只握步枪，手枪/Deagle/AWP 三系仅有单测（同一段代码、同一套规则、同一组脚链）没有真机开窗样本。

掉落弹匣停靠面恢复点（2026-09-11 20:10）：换弹掉落的弹匣此前停在**射手脚下那张水平面**上（空中换弹会停在起跳高度、贴高台边缘会停在半空）。本轮把停靠面换成**权威端按原碰撞射线取的实际地表**：`simulation.ts` 在换弹期间（`p.reload > 0`）用 `sourceLevel.groundHeight(p.x, p.z, p.y + 0.25)` 取样并写入 `Player.sourceGroundY`（无地表时回落到射手原点），该字段随权威快照自动下发（快照按整个 player 展开），四个武器系角色（AK/M4/Glock/USP/Deagle/AWP 走的 `source-character.ts` + 三个手枪/AWP 角色）都优先使用它、没有该字段时保持原行为；换弹结束即删除字段，快照不会长期多带一个数。验证：新增 `tests/source-magazine-ground-authority.test.ts` 3 条（换弹期间按原碰撞取样、把射手抬到 25 米高空后取样值仍留在原地表证明是射线结果而非脚下平面、无地表处回落射手原点、未换弹的玩家不带该字段）与 `tests/source-character-actor.test.ts` 1 条（角色优先用权威地表、掉落道具在该面上入睡、缺省仍回落到射手原点）；全量回归 **168 文件 935/935**、类型检查通过。生产 27019 双客户端 LAN 真实浏览器（`scripts/run-playwright-reload-validation.mjs` 采集器新增该字段）：host 经真实 KeyR 换弹，**49/49 个换弹采样都带权威地表**、与射手所站地面最大差 0.057m、掉落道具仍停在原寿命窗口内并按寿命移除、errors=[]。**仍缺**：飞行仍是单接触近似（不会沿地形翻滚下滑）。弹匣在飞行中会**逐帧按自己所在列**重新采样地表（`sourceMagazineDropSurface()`，由运行时把客户端预测用的原关卡 `sourceLevel.groundHeight` 经 `resolveGround` 交给渲染器；只接受不高于自身的面、无面列保留原值），因此飞出射手所在列后落到它真正飞过的那张面上；权威端在换弹期间下发的 `sourceGroundY` 仍作为生成时的面。

AWP 自己的两条特效恢复点（2026-09-12 20:10）：上一恢复点把弹壳与曳光两条链路各自接进射击路径，本轮去验收**AWP 自己的那两条**（AWP 不是"一把步枪"：`eject_brass_effect` = `weapon_shell_casing_50cal`、`tracer_effect` = `weapon_tracers_rifle`，两者都不是步枪那两条），并当场抓到一个真实缺陷：**AWP 的抛壳从来没走过发货系统**——`scene.ts` 的 AWP 抛壳回调里仍是端口自己的替身 `this.effects.eject(...)`，`sourceShells.spawn('awp', …)` 只在别的武器上被调用；表里 `weapons.awp` 一直指着 `.50cal`、单测也一直绿，因为它钉的是表而不是这条回调。现在该回调按武器自己的两个发货事实走：用哪条系统看 `table.weapons.awp`，从哪抛看 `v_snip_awp.mdl` 自带的第 2 号附件（第 1 号是枪口），怎么落看 `.50cal` 自己的重力/阻力/寿命/碰撞响应，只在系统未入库时才回落到替身。另一件被明确的是**时刻**：`.50cal` 是枪机循环把自己那枚甩出来的，比这一枪晚，所以验收等的是 `sourceAWP.brassEvents` 这个黄铜提示而不是射击事件。真机验收 `scripts/run-playwright-awp-effects-validation.mjs`（真实浏览器 + 局域网服务 + 训练局；AWP 不是出生武器，脚本**从游戏自己的商店买一把**，`vandal`→`awp`、钱 16000→11250）：`brassEvents=1`、抛的是 `weapon_shell_casing_50cal`/`shell_50cal`、初速 `3.4668 m/s` 落在自身 `2.785..3.988`（`[90,55,30]`..`[120,85,55]`×0.0254，**不是被归一化过的方向**）、`modelScale=0.001` 与表一致、**画出来的 .50 弹壳 `0.05706711 m` 与该模型自己的最长尺寸逐位相等**、0.4 s 时飞行 `1.2064 m`、`refused={}`、撞面留存 `slide 0.2 < 步枪 0.4`（三条里最紧）；曳光是 `weapon_tracers_rifle`/`particle_spark.vmt`、线长 `21.183 m` = 带子 `19.151` + 起点偏移 `2.032`（1e-6 内，即 AWP 自己的 80 单位，步枪是 20）、速度 `340.3 m/s`（自身 `330.2..355.6`）、半宽 `0.15195 m`（自身 `4..7` 单位→`0.1016..0.1778`，**不是步枪的 0.0762**）、alpha `0.5954`（`140..180`/255）、逐帧 `2.087→4.187→6.287 m` 与 `12.7/0.101×age` 相符，抓到 3 帧且每帧 `drawn` 全为真、`errors=[]`（`output/playwright/source-awp-effects-ingame.json`）。这一枪的飞行只有 `0.0563 s`（19.151 m / 340.3 m/s），带子没长满（最后一帧 age 0.0667 s，`12.7×0.0667/0.101 = 8.387 m`，与从写出的顶点量回的 8.3870 m 逐位相符）是**它自己没飞够，不是被截断**。细节见 [AWP 自己的两条特效](source-awp-effects.md)。全量 **185 文件 1083/1083**、两个 tsconfig 0 error、lint 0 error。**仍缺**：`.50cal` 自身的 `amount of bounce 0.8` 这一枪没执行到（0 次弹跳，只由表值与单测钉住）；抛壳附件是武器自己的抛壳骨（PCF 只说速度是发射器局部的，没有点名附件）；AWP 的第一/第三人称枪口是另外两条系统（`weapon_muzzle_flash_awp`），不在此列；与原件录像的逐帧对照仍然没有。

AWP 第一人称枪口闪光恢复点（2026-09-12 20:35）：上一条把 AWP 自己的弹壳与曳光验收完，本轮补上**射手自己看到的那一次枪口**。`items_game.txt` 把第一人称与第三人称写成两列，AWP 两列同名（`weapon_muzzle_flash_awp`）但画在两个地方：别人看到的在世界武器自己的 `muzzle_flash` 附件上（已实现），射手自己看到的在**视图模型自己的场景**里、挂在**视图模型自己的枪口附件**（`v_snip_awp.mdl` 第 1 号；第 2 号是抛壳）上——而后者此前从来不是原件，一直是端口自己的一个圆锥替身（亮 0.22 秒），只有手枪那一系把它藏掉。本轮新建 `game/source-first-person-muzzle.ts` 把"这一枪该画哪条第一人称系统"写成**由武器自己的 `muzzle_flash_effect_1st_person` 决定**的判据（不是按武器名写死），手枪系→`weapon_muzzle_flash_pistol`（`pistol-field`）、AWP→`weapon_muzzle_flash_awp`（`awp-chain`），其余一律返回 null；`scene.ts` 把原来的 `pendingPistolFlashes`/`sourcePistolFlash` 改成 `pendingFirstPersonFlashes`/`sourceFirstPersonFlash(weapon,mode,shot,shooter)`（两个调用点带上这一枪的 `seq` 与射手，种子按「射手 id + 其本人第几枪」推导，与世界那一路同一规则），AWP 视图模型分支按武器自己那一列取判据、用**第二个** `weapon_muzzle_flash_awp` 实例（加入 `gunScene`）在视图模型枪口附件上发射，并把端口自己的圆锥藏掉；审计新增 `sourceViewMuzzle`（含两组程序的原参数、运行计数与**渲染器自己写出的精灵行**）。真机验收 `scripts/run-playwright-awp-effects-validation.mjs`（同一发枪同时验三条系统）：第一人称 `bursts 1`、**9 个火焰精灵 + 3 个辉光精灵**（正是原件自己的 9 与 3）、锚在视图模型场景 `[0.1275,−0.0867,−1.3973]`（沿枪身 1.40 m）、附件 `1`、`placeholderVisible=false`、最远精灵 0.7624 m = 自己的 29 单位扫掠 + 1 单位出厂偏移、无一粒在枪口后方、半径与 alpha 全在自己窗口内、逐帧 1 帧（整段约 25 ms）、辅助截帧 1995 KB、`errors=[]`。新增 `tests/source-first-person-muzzle.test.ts` 4 条（按武器自己那一列取名、两家映射到各自程序而**步枪系报未入库**、缺行/改名/空名一律拒绝而不是猜一条顶替、AWP 两列同名故只需一条系统名）；全量 **186 文件 1087/1087**、两个 tsconfig 0 error、lint 0 error。细节见 [AWP 自己的两条特效](source-awp-effects.md)。**仍缺**：射手自己的**步枪**枪口闪光仍是端口的替身（步枪系第一人称与第三人称同名，但本 build 只把那套程序接进了世界场景，第一人称那一路被新判据明确报成未入库）；开镜时整支视图模型枪都不画，所以那一枪的闪光也随枪不画，原件开镜时是否仍发射该系统没有录像对照；这一路同样只读端口自己画出的东西，与原件录像的逐帧对照仍然没有。

步枪系第一人称枪口恢复点（2026-09-12 21:00）：上一条把 AWP 的第一人称闪光接上后，剩下的唯一一系——**步枪系**（AK-47/M4A4 的 `muzzle_flash_effect_1st_person = weapon_muzzle_flash_assaultrifle`，与它的第三人称列同名）此前同样一直是端口自己的圆锥替身。本轮把 `source-first-person-muzzle.ts` 的已入库集合补到三条（手枪 `pistol-field`、步枪 `rifle-vent`、AWP `awp-chain`），在 `gunScene` 里加载**第二个** `loadSourceRifleMuzzleRenderer` 实例，按武器自己那一列在视图模型自己的 `muzzle` 附件上发射，并把替身藏掉；渲染器的精灵行现在**带着自己被画出时的 `anchor`/`forward`**（`game/source-rifle-muzzle-renderer.ts`、`game/source-awp-muzzle-renderer.ts`），这样"每个精灵属于哪一次爆发"有了唯一来源——验收脚本第一次跑就是因为拿最新枪口锚点去量上一枪的旧精灵而误报 4 mm，这个字段是为消除那类错配而加的，也让 AWP 那条验收用上同一份行数据。真机验收 `scripts/run-playwright-rifle-flash-validation.mjs`（训练局出生主武器就是步枪，不需要买入；`output/playwright/source-rifle-flash-ingame.json`）：四个子系统各画出**自己整份发射**——vent 1 / glow 1 / 滚动火焰 8 / 连续火焰 9（与原 `num_to_emit` 逐项相等；总量 vent 5 / glow 3 / flame 24 / continuous 18）、glow 画在距锚点 0 处、vent 落在自己的 0.1 单位出生球内、两条扫掠沿枪管轴向前且无一粒在枪口后方、每粒半径与 alpha 都在自己窗口内、`placeholderVisible=false`、帧内 50 行精灵逐行用自己那一份锚点核对、辅助截帧、`errors=[]`。单测 `tests/source-first-person-muzzle.test.ts` 扩到 5 条（三系各自映射到自己的程序、**六把武器的每一行都被覆盖**、缺行/改名/空名一律拒绝、AWP 与步枪各只占一条系统名）；全量回归与类型检查、lint 见 PROGRESS。细节见 [原步枪第三人称枪口](source-rifle-muzzle-particles.md) 末节与 [AWP 自己的两条特效](source-awp-effects.md)。**仍缺**：开镜时（`showGun` 为假）整支视图模型枪不画，那一枪的闪光随之不画，原件开镜时是否仍发射该系统没有录像对照；同一批未执行的子系统（枪口烟 1 粒、抛壳口烟两条、火花、AWP 侧两团烟与抛壳口烟）在第一人称同样没有；第一人称这一路只读端口自己画出的东西，与原件录像的逐帧对照仍然没有。

原作逐武器击杀奖励恢复点（2026-09-12 21:20）：对照表里长期写着"逐武器击杀奖励表（发货文本里没有，现按原作默认 300）"——**这句话是错的**，数据一直在发货文件里。`scripts/items/items_game.txt` 给每把武器或它继承的 prefab 写 `kill award` 属性，**15 个 prefab 显式写着**：`statted_item_base` 300、`melee` 1500（刀）、五条 SMG prefab 各 600、四条霰弹枪 prefab 各 900、`weapon_awp_prefab` 100、`weapon_cz75a_prefab` 100、`weapon_taser_prefab` 0；其余武器顺链继承 `statted_item_base` 的 300。也就是说 **AWP 击杀 100、SMG 600、霰弹 900、刀 1500、电枪 0、其余 300**，而端口此前对每一把都发 300（AWP 击杀多给了 200）。两个独立来源对上：两份发货竞技配置都写 `cash_player_killed_enemy_default = 300`、`cash_player_killed_enemy_factor = 1`，与 `statted_item_base` 的 300 一致——导出把两者不一致当作失败，而不是二选一。解析是**走链**而不是按名字猜：`weapon_awp` 链到自己的 prefab 得 100，`weapon_ak47` 走 `rifle → primary → weapon_base → statted_item_base` 得 300，`weapon_knife` 走 `melee` 得 1500（每把武器的链都记进导出，81 把）。新增 `scripts/export-source-kill-awards.py`（Blender + 仓库自己的 SourceIO 读取器，带 ACF/build 回执）→ `research/source-kill-awards.json`，`scripts/stage-source-kill-awards.py` → 生成 `game/source-kill-award-table.ts` + 入库 `public/source/csgo-12426148/economy/kill-awards.json` + 字节/SHA 回执；"本作武器是哪把原作枪"取自**已入库的枪口效应映射**（唯一一份"本作 id → 原作 prefab"），入库时逐个核对映射到的原作武器的 `prefab` 正好是那一个；`game/source-kill-award.ts` 装载即校验（默认值与 `statted_item_base` 一致、每把武器的 award 等于它 owner 的、链末位就是 owner、映射到的原作武器存在），`sourceKillCash(gamemode, award)` 让配置只负责基数与系数，`Simulation.damage()` 现在要求说明"是什么打死的"（子弹带 `p.weapon`、手雷带 `'he'`）并按那一条发钱；同时把模式配置里那句"逐武器奖励未定义"的表述改成它真正的来源。验证：新增 `tests/source-kill-awards.test.ts` 8 条（15 条 prefab 行逐条、AWP 100/其余 300/手雷 300、本作替身武器保留基数、系数缩放、**六种自相矛盾的表装载即拒绝**、映射与效应映射逐个核对、导出与生成表一致）与 `tests/source-gamemode.test.ts` 1 条（真实 `Simulation.damage()`：AWP 击杀 **100**、步枪 **300**、手雷 **300**）；全量见 PROGRESS。细节见 [原作击杀奖励](source-kill-awards.md)。**仍缺**：本作自己的三把替身武器（VX-9 / FALCON DMR / FALCON P12）原作不存在，保留基数 300（原作 SMG 是 600，但它们不是原作的 SMG，套 600 是编映射）；系数不为 1 时没有发货文件可依（两份竞技配置都是 1），端口把系数作用在武器自己那条 award 上并把该读法写进表的 `limitations`；刀 1500 与电枪 0 只有数据没有行为（本作还没有刀与电枪）；真机只到仿真层，没有在真实浏览器里单独采一次"AWP 击杀只加 100"的样本（房间首局买不起 AWP，训练局里脚本化地一枪打死走动的机器人不可靠），如实记为边界。

天空第二层云漂移恢复点（2026-09-12 21:40）：上一轮把"云层怎么由 rate/angle/scale 组成变换"记为**未实测前不写渲染**，本轮把它量了并接进游戏。类 `CTextureScrollMaterialProxy`（客户端 `csgo/bin/client_client.so`，**32 位 ELF**，类名 0x10ef51a）的 `Init`（0xbba230）与 `OnBind`（0xbba050）逐字读出：`Init` 读四个键——`textureScrollVar`（默认**空串**，材质不点名就没有可写对象）、`textureScrollRate`（1）、`textureScrollAngle`（0）、`textureScale`（1）；代码读 camelCase 而材质文件写小写，**引擎的 KeyValues 查找忽略大小写**，这正是小写键在二进制里找不到的原因；`OnBind` 用客户端自己的 double `0.017453292519943295`（π/180）把角度转弧度、过 `sincos`（该 call 的目标在文件里是占位位移 `e8 fc ff ff ff`，**由 `.rel.dyn` 里类型 2 的重定位给出符号名 `sincos`**）、两个偏移 `u/v = frac(curtime × rate × cos/sin)`（负值加客户端自己的 1.0，落在 `[0,1)`；`curtime` 取自全局 `0x1492c10+0x10`）、矩阵 `[scale 0 0 u; 0 scale 0 v; 0 0 1 0; 0 0 0 1]` 经 `SetMatrixValue`（vtable +0x50）写入，非矩阵变量改写两分量向量（+0x30）——**角度只出现在偏移方向里，矩阵没有任何旋转项**（`scale` 是平铺缩放，不是把贴图转过去）。新增 `scripts/probe-source-texture-scroll-apply.py`（逐条断言键地址、初始值、π/180 常量、四条乘法、`frac` 两条分支、矩阵 16 槽、setter 槽、`sincos` 重定位）→ `research/source-texture-scroll-apply.json`；`scripts/extract-source-sky.py` 把材质的 `proxies` 原样带进 `sky.json`（`scrolls`），`scripts/stage-source-map.py` 重新入库（只改这一份，其余 8 个文件的 SHA 未变），新增 `game/source-texture-scroll.ts`（解析 + 实测律 + 边界），`prepareSourceSky` 装载即校验并归一成数字形状，`source-sky-render.ts` 给带 `$basetexturetransform` 的材质**克隆**底图（原贴图不动）、开 `RepeatWrapping`（发货 VTF flags `0x2000`/`0x0`，**没有** `CLAMPS`/`CLAMPT`，所以原件按重复取样）、`repeat = scale`，每帧按游戏时钟写 `offset`，并把当前 `u/v/repeat/时刻` 报进审计。真机验收 `scripts/run-playwright-sky-scroll-validation.mjs`（真实浏览器 + 局域网服务 + 训练局）：两次读数与"实测律在**渲染器自己报出的时刻**上的取值"**逐位相等（偏差 0，1e-9 门禁）**、2.52 s 内 UV 位移 0.00526、漂移方向比 **5.671281819617706** 对 `tan(80°)` 的 5.671281819617707（1e-15 内，且该判据不含任何时钟读数）、`errors=[]`（`output/playwright/source-sky-scroll-ingame.json` + 截图）。单元 `tests/source-texture-scroll.test.ts` 7 条（π/180 常量、`frac` 含负值、实测律与独立计算逐位相符、矩阵槽位与"无旋转项"、入库块解析与六种拒绝、入库 sky.json 的两块数字与底图无块、云层 alpha 与限制文本）。全量见 PROGRESS。细节见 [原作天空](source-sky.md)。**仍缺**：第二张贴图 `$texture2` 与那条 `UnlitTwoTexture` 的 alpha 分支（两条静态分支已按 token 留证：常量 1 与 `tex0.a × tex1.a × c1.a`，但选中者与 c1 上传未量出），所以云层只画底图 + 自己的 `$alpha .35`（**该结论已被后续两轮取代**：云层现在画 `tex0 × tex1 × c1`，见 [原作天空](source-sky.md)）；`$texture2transform` 同样入库但无施加对象；原雾已接（见 [原作环境](source-environment.md)）。

`UnlitTwoTexture` 静态旗标恢复点（2026-09-13 00:20）：云层那条 shader 的 alpha 分支此前只知道"两条分支都存在"（常量 1 与 `tex0.a × tex1.a × c1.a`），**不知道它在这条 shader 的什么状态上分叉**。本轮把它自己的静态旗标读出来了：`bin/linux64/stdshader_dx9_client.so` 里紧挨着 `unlittwotexture_ps20b` / `unlittwotexture_ps20` 名字之前存着一张**内联表**——`bTranslucent` + `TRANSLUCENT`、`nCustomMode` + `CUSTOM_MODE`（每对的旗标名与 combo 定义名相邻，两对按表序排列），**旗标名在整份二进制里没有任何指针引用**（只有 combo 定义名有：`TRANSLUCENT` 10 处、`CUSTOM_MODE` 2 处），说明那是一张表而不是按地址加载的字符串；引擎共享的 combo 名表里也有 `TRANSLUCENT`（1）/ `LIGHTING_PREVIEW`（2）/ `CUSTOM_MODE`（5）三条；两条静态程序点名的常量是同一组九/七个：`BaseTextureSampler`、`BaseTextureSampler2`、`cLightScale`、`g_DiffuseModulation`、`g_EyePos_SpecExponent`、`g_FogParams`、`g_LinearFogColor`——**并且两条静态自己的 CTAB 常量表逐个给出寄存器**（采样器 0/1，**`c1` = `g_DiffuseModulation`**（FLOAT4[1]）、`cLightScale` 30、`g_EyePos_SpecExponent` 11、`g_FogParams` 12、`g_LinearFogColor` 29），所以那条 alpha 乘积读的 `c1` **就是引擎自己的漫反射调制**。结论：**这条 shader 只在这两个旗标上分叉，而云层材质写着 `$translucent 1`**。新增 `scripts/probe-source-unlittwotexture-combos.py` → `research/source-unlittwotexture-combos.json`（含旗标表偏移、两对名字、引用计数、共享 combo 表、两条静态的常量名、两条 alpha 分支的既有 token 结论）。**没有落地渲染**，因为还剩一件没量出来的事：`bTranslucent` 在静态索引里占**哪一位**（表序可读、索引合成方式未读）——在这件读通之前，云层仍只画底图，第二张贴图不画。全量 188 文件 1104/1104、两个 tsconfig 0 error、lint 0 error（本次只新增探针与文档，未改运行时）。

弹着贴花的雾衰减与池子恢复点（2026-09-13 01:05）：贴花那条链的"仍缺"里长期挂着"原 `Subrect` 深度羽化/`$fogfadeend` 雾衰减/原 `r_decals` 预算与淡出时间"，三件都没有发货依据。本轮把其中两件量清楚：**雾衰减是发货 `DecalModulate` 自己的功能**——`bin/linux64/stdshader_dx9_client.so` 里该 shader 的旗标表紧挨着 `DecalModulate_DX9` / `DecalModulate` / `decalmodulate_vs20` 与 `decalmodulate_ps20b/ps20/vs30/ps30` 名字，写着 `bHasFogFade` + `FOGFADE`（还有 `nLightingPreviewMode` + `LIGHTING_PREVIEW`），随后是四个**连描述都随包下发**的参数：`$FOGEXPONENT`（默认 `0.4`，"exponent to tweak fog fade"）、`$FOGSCALE`（"scale to tweak fog fade"）、`$FOGFADESTART`（"fog amount at which to start fading decal"）、`$FOGFADEEND`（"fog amount at which to end fading decal"）——shader 写**大写**、材质写小写，靠引擎忽略大小写的查找对上（与 `texturescrollvar` 同一个套路）；**14 个用到它的材质全部只写 `$fogfadeend 0.5`**（7 张 `decals/blood*.vmt`、`decals/decals_bulletsheet.vmt`、6 张 `decals/flesh/blood*.vmt`），**本移植画弹着用的那张 `decals_bulletsheet.vmt` 就在其中**。**池子**：`r_decals` 是真实 convar，发货默认是紧挨着它名字的那个字符串 **2048**（`bin/linux64/engine_client.so` 字符串池 `"2048\0r_decals\0"`），引擎自带调试串 `%d decals: %d permanent, %d dynamic\nr_decals: %d` 与 `R_FindDynamicDecalSlot: no slot available` 说明它把贴花分成**永久**与**动态**两类、满了回收槽位；**全仓扫过没有任何 `cl_decals*` 淡出 convar**，所以"淡出时间"这一项在发货件里根本不存在。新增 `scripts/probe-source-decal-pool.py` → `research/source-decal-pool.json`（逐条断言：邻接默认串、调试串、四个参数名与描述、`$FOGEXPONENT` 默认 0.4、14 个材质全为 `$fogfadeend 0.5`、每个二进制都没有 `cl_decals*`）。**没有落地渲染**：雾衰减那条程序的算式还没读出来；而**本移植现在已经接上地图自己的雾**（`env_fog_controller` 的 `512/9000/.4`，见 [原作环境](source-environment.md)），所以这条衰减现在**会**看得见——算式一旦读出就必须画；池子仍用自己那个 512 张的有界池，理由是**每个弹着一张 quad、2048 张就是 2048 次 draw call**，这一点在 `game/source-impact-decals.ts` 的模块注释与 `docs/source-impact.md` 里如实记为差异而非对齐。同轮把 `docs/source-impact.md` 里"弹壳与曳光仍只有原映射未实现"这句**早就不成立的话**改掉（两者都已真机验收，指向各自的文档）。全量 188 文件 1104/1104、两个 tsconfig 0 error、lint 0 error（本轮只新增探针与文档，未改运行时逻辑）。

任务开始时未发现本机 CS:GO 安装。目前已用 Valve 官方 SteamCMD 匿名完成 App 740 下载与校验（2023 年构建 12426148），安装 34,705,746,562 字节；最终 `Success!`、退出码 0、ACF StateFlags 4 与两个 depot manifest 已读回，见 `research/csgo-install-receipt.json`。原始文件保存在 `.reference-assets`，来源与工具链见 `research/official-csgo-assets.md`、`research/source1-to-glb-toolchain.md`。当前 AK 来自该安装的骨骼模型与原动画；早期官方工坊的 34 把静态 OBJ 仍只是私有参考。

官方 Source SDK 2013 不能当作已经取得 CS:GO 源码。R3 的 Source Phong 材质以固定 SDK 公式为有界实现，尚未复原 CS:GO 完整 shader、局部环境光和曝光。现有 C02/港口素材来源见 `public/models/web-w01/CHARACTER-NOTICES.md`、`LICENSE-Rocketbox.md` 及地图 provenance；没有将它们描述为 Valve 原作资产。

应以固定原作版本、同视角录像、动作时间点、弹道与地图量测来判断“一样”。在取得这些对照前，只能准确报告当前实现和可见差异，不能声明 100% 还原。

`UnlitTwoTexture` 云层分支与第二张贴图恢复点（2026-09-13）：上一轮读出了这条 shader 的静态旗标，但"云层落在**哪一条**"仍记成没量出来（`bTranslucent` 占静态索引哪一位未知）。本轮不再去解索引的合成方式，而是用"只声明 `$translucent 1` 的材质能落在哪些键上"把结论钉死：容器里正好 10 条静态键（`0x0 0x1 0x2 0x4 0x6 0xc 0xe 0x12 0x18 0x1e`），**恰好一个旗标置位的只有 `0x1`／`0x2`／`0x4`**，而其中**只有 `0x1` 读两个采样器**（`0x2` 是不读贴图写常量色、`0x4` 是不读贴图的预视分支），所以一个"两张贴图"的材质只可能是 `0x1`——不需要索引合成方式。该静态的 4 条动态里**只有 `dynamic 1` 完全没有雾常量**（`c11`/`c12`/`c29`），也就是 `$nofog 1` 拿到的那条；`0x1/dynamic 1` 的程序逐行读出：`r0 = tex0 × tex1; r0 = r0 × c1; r0.rgb ×= c30; oC0 = r0`——**alpha = tex0.a × tex1.a × c1.a**，`c1` 是 `g_DiffuseModulation`（材质颜色与 `$alpha`），`c30` 是 `cLightScale`（CTAB 逐条给出寄存器，两条静态一致）。第二张贴图 `$texture2` = `nuke_clouds_001` 也按原 VTF 无损导出：256²、RGB 243–252、**alpha 0–209**（云形在 alpha 里），正好是"透明分支"才有意义的那张图。落地：`scripts/probe-source-cloud-layer-branch.py` → `research/source-cloud-layer-branch.json`（逐条断言）、`scripts/export-source-sky-cloud-texture.py` → `sky/cloud-texture2.json`（字节/SHA/通道统计）、`sky.json` 增加 `second`/`translucent`/`program` 且 `prepareSourceSky` 装载即校验（含 `$nofog` 与动态一致、第二张贴图的字节/SHA/尺寸/夹取位）、`stage-source-map.py` 入 manifest 的 `skyTexture2`、`game/source-sky-cloud-loader.ts` 按回执校验后解码（sRGB、`flipY=false`、无夹取位→重复）、`game/source-sky-render.ts` 在 `map_fragment` 之后把第二个采样乘进 `diffuseColor`（three 的 `diffuseColor` 初值恰是 `c1`）并把这条层报进审计。真机 `scripts/run-playwright-cloud-layer-validation.mjs` 证明**实况帧确实由这份材质绘制云层穹顶**（审计双贴图层 + 这一帧恰好编译 1 个本移植 cache key 的程序）。**已补上**：同一次运行里的"逐像素复现 `tex0 × tex1 × c1`"复现出来了——1,021,440 个像素全部在 2/255 内（最差 0.996、平均 0.897），前提是把三遍都关掉影调映射：实况渲染器开着 **ACES 影调映射**而移植保留材质的 `toneMapped`，所以"装了乘积的材质"与"没映射的参考贴图"本来在两个域里（这是上一轮"没复现"的原因，不是发布代码有问题）。比较的是 **RGB**：该上下文的绘图缓冲不携带片元 alpha（不透明的红色探针也读回 255），alpha 乘积由代码与装载/契约测试保证。**仍未做/未证**：原 GPU 实际选的 mip／各向异性／混合状态。程序尾部的 `cLightScale`（`light_environment._lightscaleHDR` = **1**）与地图自己的雾（`env_fog_controller` 的 `512/9000/.4`）**都已施加并真机逐像素验收**，见 [原作环境](source-environment.md)。
