# CS:GO Web Implementation Plan

> **For agentic workers:** Use scoped parallel implementation and independent review; preserve this progress ledger across continuations.

**Goal:** 推进完整 CS:GO 网页复刻目标，交付首轮真实运行的独立局域网版本及明确差异。

**Architecture:** 冻结基线，浏览器渲染及输入预测，权威服务器处理规则。皮肤只修改展示状态，LAN 启动器管理自己的进程。

**Tech Stack:** TypeScript、React、Three.js、Rapier、Colyseus、Node 22。

**Spec:** `docs/superpowers/specs/2026-09-08-csgo-web-design.md`

## Global Constraints

- 仅写本任务 csgo-web；旧工程只读。
- 新默认端口 27015；共享协议 csgo-web-r1。
- 不将同机验证称跨设备通过，不将原创资源称原作资源。
- 新增皮肤保持骨骼、几何、枪械性能与非涂装表面。
- 新产物必须构建并真实读取，不继承旧报告的通过状态。

## Task 1: 独立基线与缺口

- [x] 提取 W04-r3 冻结源码和静态资源，保存来源与 SHA。
- [x] 初始化隔离 Git 分支，补齐冻结归档漏掉的 use-mobile hook。
- [ ] 独立审计资源/网络/原作基准，形成差异清单。

## Task 2: LAN 入口（network_audit）

Files: `server/index.ts`, `game/protocol.ts`, `scripts/lan-service.mjs`, 两个 `.command`, `docs/lan.md`，专用网络验证脚本。

- [ ] 当前网卡枚举与端口冲突保护；启动成功读回、停止身份检查。
- [ ] 提供 `/api/server-info` 实际地址与版本。
- [ ] 创建房间→第二客户端加入→同步→退出清理，记录实际命令和结果。

## Task 3: 皮肤与武器检视（root）

Files: `game/skins.ts`, `game/weapon-inspect.ts`, `game/scene.ts`, `game/runtime.ts`, `app/page.tsx`, `app/globals.css`, 对应单测。

- [ ] 使用显式可涂装材质/网格选择，克隆每件武器的材质，保留默认材质可还原。
- [ ] 提供至少 5 款原创涂装，通过武器库实际预览并本地持久化。
- [ ] 平滑检视动作，F 触发，开火/换弹/切枪自动退出，检视状态不参与服务器命中。
- [ ] 核对材质无共享污染、资源生命周期、换肤后刷新和进入游戏。

## Task 4: 构建、浏览器与差异记录

- [ ] `npm run typecheck`、必要规则单测和新增行为单测、双端构建。
- [ ] 新端口实际网页资源和 HTTP/WS 读回；真实浏览器图像和可用输入循环。
- [ ] 独立评审新增改动，修复关键问题。
- [ ] README 给实际地址/命令、已实现能力和完整原作一致性剩余差异。

## Progress

2026-09-08：完整目标 active。首轮基线 e725c48。独立读取原项目表明其仍未完整验收，不能继承为本任务完成。研发目录：系统操作/csgo-web。
